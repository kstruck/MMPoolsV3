# ☀️ MORNING 2026-07-24 — takeover

**Target: the Hall of Fame game, 2026-08-06.** 13 days out.

**No production data was mutated overnight. Nothing was deployed.** Everything
below is docs/plans in open PRs for your review.

---

## 0. The 90-second version

| # | Thing | Your move | Where |
|---|---|---|---|
| 1 | **Fix codex** — it broke overnight, blocks ALL code review | Account/config | §1 |
| 2 | **Approve the stats plan** (#267) — big, and it reverses yesterday's date-cutoff | Read + 3 decisions | §2 |
| 3 | Merge the two deploy-state docs PRs (#268, and #267 after approval) | Merge | §3 |
| 4 | Reads verification — did the drop land? | Query Insights | §4 |
| 5 | Two doc-cleanups still awaiting your word (PLAN-LOOPS, audit harvest) | Yes/no | §5 |
| — | **A8 pricing — due 2026-08-06** | Yours | — |

**Headline:** your "drop reminders to 15m" shipped and is live; the two `.env`
deploy fires are out; the frontend is finally caught up (profile fix live). The
**stats work is now a real plan** (#267) — and your Q4 answer quietly killed the
date-cutoff approach, which is decision #1 for you.

---

## 1. ⚠️ codex is DOWN — fix this first, it blocks everything

Codex reviewed #265 and #266 fine yesterday, then broke overnight. Every review
now fails with:

```
Project `proj_mZrROfq3w41f8FNHDCxgCb3q` does not have access to model `gpt-5.6-sol`
```

plus a corrupted models cache (`missing field supports_reasoning_summaries`).

**Root cause:** `~/.codex/config.toml` pins `model = "gpt-5.6-sol"`, which is a
**ChatGPT-plan model**. The API key you added belongs to an OpenAI **project**
that doesn't have access to it. (It worked earlier because codex was still on
your ChatGPT auth; once it's fully on the API key, that model is unreachable.)

**Options, any one fixes it:**
- Grant that OpenAI project access to `gpt-5.6-sol` (OpenAI dashboard → project → model access), OR
- Point codex at a model the project DOES have — edit `~/.codex/config.toml`, set `model` to an available API model (e.g. a `gpt-5*` your key can use), OR
- Switch codex back to ChatGPT auth (`codex login`) — but that's the rate-limited plan that resets ~Jul 28.

I did **not** touch your codex config (it's your account). I cleared the
regenerable cache to rule that out; it wasn't the cache.

**Why it matters:** per CLAUDE.md §2c, code PRs don't ship without a cross-model
review. With codex down and qodo billing-blocked, I have **no reviewer**. I can
keep writing docs/plans, but I should not ship the stats *code* (money +
world-readable doc) unreviewed. Fixing codex unblocks the whole A→F build in §2.

---

## 2. The stats plan — #267, needs your approval

[PR #267](https://github.com/kstruck/MMPoolsV3/pull/267) adds **§8 of
`PLAN-STATS-INTEGRITY.md`** — the approval-ready plan built around your Q2/Q3/Q4
answers. Read §8; it supersedes §4/§4b.

### The one that needs your head: your Q4 answer killed the date cutoff

Yesterday (Q6) we chose "count only pools created on/after 2026-09-09" to exclude
your preseason testing. **Your Q4 answer breaks that:** you want NFL playoffs
(Jan 2026) and March Madness (Mar 2026) *included* — and those are **before** the
cutoff. A date cutoff would delete them.

So §8 drops the date cutoff and instead excludes **test pools directly**:
`isSimPool` OR NFL `seasonType == 1` (preseason = your testing). Real pools count
regardless of age. This is cleaner and matches all three of your answers — **but
it reverses a decision you made yesterday, so I need your explicit OK.**

### Three things open the work (§8.7)

1. **Sign off on dropping the date cutoff** for the `isTestPool` discriminator.
2. **Confirm the Q3 read:** I interpreted "let the user hide the stats" as a
   **per-user profile visibility toggle** (public/private on `/profile/:uid`),
   because one user can't hide the site-wide `stats/global`. If you meant a
   site-wide admin toggle instead, say so — smaller build.
3. **Run the census** (§8.2) — a read-only prod enumeration so we know whether
   any unmarked legacy test pools exist before assuming the discriminator is
   complete. It needs your creds; I can't run it.

### What's still true regardless (the money bugs)

The numbers are wrong until these land, independent of the above: `calculatePoolPot`
returns **0** for NFL + Props pools; the recompute never visits NFL pools; paid
state lives in Member Records, not entry docs. All in §8.3.

### Shape of the build

Six PRs A→F, one at a time (§2d cadence), each plan-gated and codex-reviewed
(hence §1 first). The Recalculate + Coolify triggers are yours, handed to you at
the right points. I did **not** start building — it's plan-gated and you asked
for a plan to approve.

---

## 3. Merge queue

- **#268** — deploy-state: frontend caught up. Docs-only, CI green, safe to merge now. (I verified the profile fix is live: header+footer render on `/profile/:uid`, new bundle `index-BhilVMpo.js`.)
- **#267** — the stats plan. Merge **after** you've read §8 and given the three answers in §2 (merging it doesn't start code — it just records the approved plan).
- **#133** — tailwindcss 4.x dependabot, still open, still low-priority. Leave it.

Both #267/#268 are **not codex-reviewed** (codex is down) — flagged in each PR. They're docs; the docs-state invariant guards the load-bearing part.

---

## 4. Reads verification — still worth doing

#262 + #265 should have cut the `runReminders` read load hard. Check
**GCP → Firestore → Query Insights**, 6-hour window: the row
`nfl_games WHERE (season = ? AND startTime > ?)` should read **~24 executions/6h**
(down from ~780). That's the clean per-query signal; the total-reads graph mixes
in ~340–440K/day of unattributed queries this fix doesn't touch, so judge on the
`nfl_games` row, not the headline number. If it's still ~780, tell me — the fix
didn't take.

---

## 5. Two doc-cleanups still awaiting your word (from yesterday)

Neither done — both need your go:

- **`PLAN-LOOPS.md` → delete.** Verified fully superseded: all 5 loops it planned are now `mmp-loop-*` skills. Zero loss. (Untracked in your main checkout — a plain `rm`.)
- **`HARNESS-MODEL-AUDIT-2026-07-16.md` → harvest, then delete.** ~Half its findings are already absorbed; the rest is a small memory-cleanup pass (2 stale memory files to delete, a model-tiering note to fix). I'll harvest those into the right layers, then delete the 164K snapshot.

Say "do both" and I'll handle them (memory edits are your self-model, so I held).

---

## 6. What shipped / was verified overnight

- **15-min reminder cadence** — #265, deployed, live. codex caught (before it broke) that the 12-min BRACKET windows would be silently missed at 15-min polling; widened to 24 min, mutation-tested.
- **Two `.env` deploy fires, both out:** the dotenv parse error (stray todo-notes) and the Stripe secret/plain-env overlap. `functions/.env` is now comment-only with a note so it can't recur. Both Stripe functions redeployed clean.
- **Frontend caught up** — #266 merged, Coolify rebuilt, #261 profile fix verified live.
- **Deploy-state marker** at `49c12a9`, all cadence prose reconciled across skills + KEVIN-TASKS (#266). Fixed a self-referential secret-scan landmine as a bonus.

---

## 7. Standing engineering queue (unblocked, once codex is back)

1. **`sendEmail` / `sendCourierSMS` outcome plumbing** — `runReminders` can't see delivery failures its helpers swallow. Own careful PR (delivery path).
2. **Scheduled Auth export** (`PLAN-BACKUPS-PHASE3.md` step 6) — blocked on you creating the GCS bucket + IAM.
3. **The bounded-query follow-up to #262** — composite index + `limit(1)`, another ~10× on reads. Needs an index built before the code ships (you deploy the index).

All three want codex, so they queue behind §1.
