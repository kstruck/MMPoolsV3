# MORNING — 2026-08-15 — overnight run report + your task list

Read this first. Everything is numbered. Every step says where to be, what to
type, what you should see, and what to do if you don't. Your shell is
PowerShell 5.1: one command per block, no `&&`.

---

## 0. The one-paragraph summary

Overnight I shipped **4 PRs, all gate-complete and merge-ready** (#434 CI gates,
#435 item 8, #436 the three plans, #437 item 14), built a fifth (**T0**, the
multi-entry row-identity guard) that is committed and waiting for #437's qodo
before it opens, and wrote the **three plan-gated plans** (multi-entry,
co-commissioners, payment ledger) with review logs and sweeps — **51 codex
findings across 12 rounds, all 76 accepted or answered, none rejected, none of
the three reached APPROVED** (details in §3; that is honest, not a failure).
Nothing was deployed; nothing touches `functions/` except a CI test runner. A
read-only browser pass on #430/#432 in prod found **11 oddities** (§5). Two
things need your ruling before anything else builds: the **§3 decision tables**
and the **item 13 choice** (§4).

⚠️ **The ordering risk the brief warned about is real and is now a rule with a
test**: `PLAN-MULTI-ENTRY.md` §0b + T0 (`claude/t0-entry-keyed-reveal`). Items
9, 11, 12 must not be built until T0 merges — see §6.

---

## 1. What shipped — per PR, with gate evidence

| PR | Title | Gates | Deploy owed |
|---|---|---|---|
| **#434** `claude/ci-rules-tests-qodo-anchor` | The eight `functions/scripts/*.rules.test.mjs` run in CI (`npm --prefix functions run test:rules`, one emulator, wiped between files, fails on < 8 files); qodo watcher's settle floor anchored on the `Code Review by Qodo` comment (measured on #432: summary → review = **16.5 min**; the old anchor printed a false clean twice) | codex clean ×2; qodo 5 findings → 3 absorbed (timeouts, loopback guard, wrapping), 2 rejected with reasons on the PR; CI green incl. `8/8 rules test files passed` in the emulator-tests log | **none** — CI + test runner + skill docs |
| **#435** `claude/item8-pick-cta-agree` | Item 8: every pool-home picks button derives its label from `pickCtaFor` (open+incomplete → **Make Picks**, open+complete → **Edit My Picks**, locked+picks → **View My Picks**, locked+none → **Picks Locked**); honours `weekLockOverrides` on Pick'em; the third hardcoded "Submit My Picks Now" is gone; the invariant test was **deliberately rewritten** (it asserted the literal `'Make My Picks'`) | codex 3 rounds (r1 found the override gap → fixed; r2, r3 clean); qodo 1 bug (disabled state during load → absorbed: the button is never disabled) + 3 style rejected; CI re-running on the last push at time of writing | **one Coolify rebuild** |
| **#436** `claude/plans-multi-entry-cocomm-ledger` | The three plans + review logs + sweeps (§3) | codex clean (docs); qodo 1 style rejected; CI green | **none** |
| **#437** `claude/item14-pool-card-type` | Item 14: My Entries cards show the pool TYPE (Pick'em/Survivor/Margin/…) and its OPTIONS (Straight-up/ATS, Confidence, Season-long/Weekly/Hybrid; Survivor strikes/rebuys); NFL cards get a cost display; the fake "Your Entries 0" line is hidden for NFL types | codex r1 clean; 1068 tests; qodo pending at time of writing (hand-poll — see §7 step 3) | **one Coolify rebuild** |
| **T0** `claude/t0-entry-keyed-reveal` (branch, no PR yet) | `PLAN-MULTI-ENTRY` T0: the three uid-keyed reveal lookups flip to `row.id`; a source invariant over 10 NFL surfaces forbids six uid-keyed shapes with a per-symbol allow-list | 1076 tests, tsc clean; codex not yet run | one Coolify rebuild (behaviour-identical) |

Also open from before: **#433** (HANDOFF catch-up, docs, still open — merge owes nothing), #380 README, dependabot ×6.

**Not started (in order):** items 10, 12, 11, 9 (need T0 first), 7 (nav), 13 (needs your ruling, §4), 5/6/4 (plan-gated, §3), 2 and 3 implementation (plan-gated, §3). Also `PLAN-EMPTY-SUBMISSION-FEE` still unsigned and multi-entry's K9 wants it first.

---

## 2. Merge order and deploy runbook (all four are independent; this order is safest)

**Header:** merge #434 → #436 → #435 → #437, then one Coolify rebuild, then the sentinel crawl. No functions or rules deploy is owed by anything tonight.

1. **Where:** `D:\march-melee-pools` (the main checkout, not a worktree).
   **Run:**
   ```powershell
   git fetch origin
   ```
   **See:** no error. **If not:** check network / `gh auth status`.

2. **Confirm each PR is still green before merging** (CI re-runs on every push; #435 had a late push):
   ```powershell
   gh pr checks 434
   ```
   ```powershell
   gh pr checks 435
   ```
   ```powershell
   gh pr checks 436
   ```
   ```powershell
   gh pr checks 437
   ```
   **See:** every row `pass`. **If a row says `fail`:** open the URL it prints, read the failing step, and stop — do not merge that PR; tell me which step.

3. **Confirm qodo has reported on #437** (it had not when I wrote this):
   ```powershell
   gh api --paginate repos/kstruck/MMPoolsV3/issues/437/comments --jq ".[] | select(.user.login==\"qodo-code-review[bot]\") | .body[0:80]"
   ```
   **See:** a line starting `<h3>Code Review by Qodo</h3>` (a `PR Summary` alone is NOT the review — that is the whole point of #434's watcher fix). **If absent:** toggle it — `gh pr ready 437 --undo` then `gh pr ready 437` — and wait ~2 min, re-run. **If it reports findings:** paste them to me before merging; I will absorb or reject each with reasons on the PR, as I did on #434/#435.

4. **Merge, in order** (squash, delete branch):
   ```powershell
   gh pr merge 434 --squash --delete-branch
   ```
   ```powershell
   gh pr merge 436 --squash --delete-branch
   ```
   ```powershell
   gh pr merge 435 --squash --delete-branch
   ```
   ```powershell
   gh pr merge 437 --squash --delete-branch
   ```
   **See:** `✓ Squashed and merged pull request #…` each time. **If a merge is refused for conflicts:** stop at that PR, tell me which one; the others are independent and can still merge.

5. **Rebuild the frontend** — Coolify dashboard → the `www` app → **Redeploy** (pushing to `main` does NOT auto-deploy the frontend). Wait for the build to finish (green).

6. **Verify the deploy inside the shipped JavaScript, not by a moved hash.** Where: `D:\march-melee-pools`. Run the chunk-graph crawl from `mmp-deploy-and-operate` with THESE sentinels (they exist only after tonight's PRs):
   - `Picks Locked` — expect count **1** (#435)
   - `Submit My Picks Now` — expect count **0** (#435 removed it)
   - `Against the spread` — expect **1** (#437)
   - `Sudden death` — expect **1** (#437)
   **See:** those four counts. **If `Submit My Picks Now` is still 1 or `Picks Locked` is 0:** the rebuild did not pick up `main` — redeploy again from Coolify and re-crawl; do not assume.

7. **Eyeball in prod (2 minutes):** open https://www.marchmeleepools.com/pool/0ybpLzY7fJ3NJbDj0j1l — the banner and the card should now use the SAME words for the same week; the bottom strip button should read the same as the card. Then https://www.marchmeleepools.com/participant?tab=entries — each NFL card shows a type pill (Pick'em / Survivor / Margin) and its options, and no "Your Entries 0" on NFL cards.

8. **HANDOFF top box:** #436 does not update HANDOFF (I did not want to fight #433's open catch-up). After merging, tell me and I will write the top box, or merge #433 first and I will rebase a small HANDOFF PR on it.

---

## 3. 🛑 DECISION TABLES — the point of the night. Answer these; nothing plan-gated builds until you do.

All three plans are in #436 (`PLAN-MULTI-ENTRY.md`, `PLAN-CO-COMMISSIONERS.md`,
`PLAN-PAYMENT-LEDGER.md`), each with a `-REVIEW-LOG.md` and `-SWEEPS.md`. Every
row below has my recommendation; **write your answer next to each K** (or say
"all recommendations") and I build in that order.

### 3a. Codex Act 2 outcome — read this before the tables

Grill-with-docs-codex Act 1 ran without you (no one to interview), so the
questions the grill would have asked ARE the tables. Act 2 (codex, read-only,
fresh session per round):

| Plan | Rounds | Findings | Accepted | Rejected | Verdict |
|---|---|---|---|---|---|
| Co-commissioners | 5 (the cap) | 28 | 28 | 0 | **REVISE at cap — deadlock recorded.** Trajectory 10→8→4→3→3; rounds 4–5 were placement/wording; the design has been stable since round 3. |
| Multi-entry | 4 | 32 | 32 | 0 | **REVISE — stopped.** 14→8→5→5; round 4 still found a real money defect (PAID member adds an entry → dues rise but status stays PAID → "collected" lies), now fixed in D2/K11. |
| Payment ledger | 3 | 16 | 16 | 0 | **REVISE — stopped.** 9→3→4; all round-3 items were inside decisions already taken. |

I stopped short of APPROVED on purpose: codex is paid, none of it ships before
you sign, and the next real findings will come from reviewing T-ticket CODE.
The review logs record each round's findings, my verdicts, and my
counter-position at the stop. **What you should take from it:** every one of
the co-commissioners plan's three "safety by ordering" claims was wrong on
first writing and caught by codex, none by me — the deploy order in that
plan's D2/§7 is the load-bearing part.

Two tooling facts, so nobody re-learns them: `codex exec --json … | grep
thread.started` (the skill's resume path) **hung for 30 min at 0.1 s CPU** on
this machine — abandoned for fresh sessions per round; and a backgrounded
`codex exec` **must have `</dev/null`** or it blocks on "Reading additional
input from stdin".

### 3b. PLAN-MULTI-ENTRY — item 3 (money + scoring)

| # | Question | Recommendation |
|---|---|---|
| **K1** | Entry identity: `e${n}:${uid}` with entry #1 = `uid` (zero migration; index PREFIX because sim/test uids contain `_`) vs Bracket-style auto-ids? | **`e${n}:${uid}`.** |
| **K2** | Upper bound on the wizard's max? | **10.** |
| **K3** | Dues: `feeOwed = fee × max(joinLiability, playableEntryCount)` — a member owes one fee at join as today, more when extra entries commit a pick; ONE Paid Status per member until the ledger plan? | **Yes.** |
| **K4** | Survivor: two entries of one player MAY pick the same team the same week? | **Yes.** |
| **K5** | Entry names default `"Name #2"`, member-editable on My Entry (via `submitNFLPicks`), unique per member? | **Yes.** |
| **K6** | Commissioner can turn multi-entry ON / raise the max on an existing pool? | **Yes while the pool accepts entries; raise-only; never lower.** |
| **K7** | Member can delete an entry (before picks)? | **No, v1.** |
| **K8** | One player can take 1st AND 2nd in a week with two entries? | **Yes.** |
| **K9** | Ship `PLAN-EMPTY-SUBMISSION-FEE`'s one-liner FIRST as its own tiny plan-gated PR (it is live money today and multi-entry would multiply it)? | **Yes, first.** |
| **K10** | Second entries count as entries for the pot and hybrid split? | **Yes.** |
| **K11** | A PAID member who adds an entry flips back to UNPAID with a ledger line (their `feeOwed` just rose)? | **Yes** — anything else reports money as collected that was not. |

**Build order if signed:** T0 (already built) → K9's tiny PR → T1 schema/wizard → T2 submit+dues → T3 scoring/reveal/finalize/profile → T4/T5 client → T6–T11.

### 3c. PLAN-CO-COMMISSIONERS — item 2 (authorization)

Two facts you should know before the table: (1) the brief said `coManagers` is
"referenced in `firestore.rules`" — **it is not** (`grep -c coManagers
firestore.rules` → 0); (2) it **is client-writable today** (not in
`protectedFieldsUnchanged()`), and three functions gates already honour it — so
**T1 is a LOCK that should ship regardless of the feature**, and the plan's D2
deploy order (functions ignore the field → rules lock + scrub → callable +
widened gates + rules readers) is what makes it safe.

| # | Question | Recommendation |
|---|---|---|
| **K1** | Accept the §3 capability table (C1–C13) as written? Edit any Yes/No. | Accept. |
| **K2** | Ship T1 (the lock) on its own, now, before the feature? | **Yes.** |
| **K3** | C6 money: co-commissioner may mark members paid and record payouts? | **Yes.** |
| **K4** | C7: co-commissioner sees pre-lock "14 of 16 Picks Set" once `coManagers` is server-owned? (flips two pinned emulator tests deliberately) | **Yes.** |
| **K5** | Cap per pool? | **3.** |
| **K6** | Must be a member (canonical Member Record)? | **Yes.** |
| **K7** | Squares PII readable by a Squares co-commissioner? | **No** (and NFL-only in v1 anyway — C13). |
| **K8** | Prose name **Co-Commissioner**; field stays `coManagers`. | Yes. |

### 3d. PLAN-PAYMENT-LEDGER — items 4 + 6 (+5) (money)

🛑 **This plan stands on `PLAN-WEEKLY-PRIZES.md`, which is UNSIGNED** (D1–D8
there, no review log, no sweeps). The ledger's "owed for each week they won" IS
that plan's `weeklyPlaces` + frozen pot. **K1 asks you to sign both.**

| # | Question | Recommendation |
|---|---|---|
| **K1** | Sign `PLAN-WEEKLY-PRIZES.md` D1–D8 too (its recommendations stand)? | **Yes.** |
| **K2** | Item 6: new `settings.weeklyPayouts` for HYBRID's weekly places; `settings.payouts` unchanged for SEASON/WEEKLY (absent `weeklyPayouts` = today's behaviour)? | **Yes.** |
| **K3** | "Owed" is a displayed estimate; ticking the box RECORDS a settled Payout Record; nothing auto-recorded on scoring? | **Yes** — the money invariant. |
| **K4** | Allow a **weekly** `PLACE` award once that week is scored (relax "pool must be FINAL" for weekly only)? | **Yes.** |
| **K5** | Un-tick = flip `settled` (new `setPayoutSettled`), amount immutable, supersede to correct? | **Yes.** |
| **K6** | Pot from every entry or PAID only? | **Every entry**, printed. |
| **K7** | Members see only their own prize rows + the pot? | **Yes.** |
| **K8** | Item 5 (move the hybrid-split control under Entry Fee) ships first as an ORDINARY PR with the interaction-test proof? | **Yes.** |
| **K9** | Unique ranks enforced on both lists (census first)? | **Yes.** |
| **K10** | Weekly places + amounts on the publicly readable recap — public product data, or gate the recap? | **Public**, stated on the page. |
| **K11** | Deterministic weekly award id `wk{week}-{entryId}-p{place}`, bound to the recap's place + frozen amount; overrides via BONUS/ADJUSTMENT? | **Yes.** |
| **K12** | After a rescore a mismatching recorded award shows STALE and is re-recorded by supersession on click — never auto-corrected? | **Yes.** |

---

## 4. Item 13 — "No Points" → "Incorrect": your choice

The column was renamed the OTHER way on purpose (#427/#429): the figure is
`total − correct`, which counts pushes and voids — a tie or a cancelled game is
not "incorrect". You are ruling for "Incorrect" and your reference site uses
that word. Two ways to do it:

| Option | What | Cost | Honesty |
|---|---|---|---|
| **A — extend the projection (recommended)** | `buildStandingsRows` adds `incorrect`, `push`, `void` counts to the per-week `weeklyResults` summary (allowlist, no per-game map, so no leak); the column shows the REAL incorrect count | a `functions/` change → **functions deploy into the LIVE scorer**; the row is written on the next score of each week, so past weeks show "—" until re-scored (or one manual `scoreNFLWeek` re-run per pool) | the number is right |
| **B — rename and accept** | header says "Incorrect", figure unchanged | frontend only | reintroduces the exact defect #429 removed: a player with 1 correct + 1 push prints as 1 incorrect |

**Say A or B.** If A: it is a scoring-projection change → ordinary in class (no
money/auth/data), but it deploys into the live scorer, so I will do it as its
own PR with the PR body saying so.

## 4b. Item 15 is empty

Your list ends at "15." with nothing after it. Was something lost? Tell me the
item and I will slot it.

---

## 5. Browser pass on #430/#432 in prod (read-only, your account) — findings

Full report in the session; the actionable list:

1. **Pool Home CTAs disagreed on every NFL pool** (banner "MAKE PICKS →" for the current week; card "EDIT MY PICKS ›" for a locked past week; third hardcoded button) — **fixed by #435**.
2. **My Entries cards: "Your Entries 0"** on pools where you are ranked #1 — **fixed by #437** (hidden for NFL types; the card never reads entry docs).
3. **Margin standings formatting:** NEGATIVE BURDEN renders `-0`; BEST WK renders `+-3` — /pool/2mv4pKI734hHeQHzQVTP?tab=standings. Not fixed; small, needs a sign helper.
4. **Survivor grid legend** says results are "reported in Standings and Results", but a Survivor pool has no Results tab — copy fix.
5. **`?tab=results` on a Survivor pool** renders Pool Home while the URL keeps `tab=results` — the fallback works (not blank, as tested), but the URL should probably be rewritten to `dashboard`. Small.
6. Results "SEASON SUMMARY" sub-tab took three clicks to switch once — not reproduced deliberately; possibly a screenshot-timing artefact.
7. Season Summary / Margin Summary week columns are headed `1 2 3 4` while every other surface names weeks ("HOF Weekend", "Preseason Week 1").
8. `?week=` numbering: `week=1` = HOF, `week=2` = Preseason Week 1 (your example URL `week=2` opened Preseason Week 1) — by design (season week numbers), but worth knowing when you paste URLs.
9. HOF Weekend picks carry confidence weight 16 on a one-game week (all players 16; the HOF win added 16 to totals). Is that intended for a one-game slate?
10. Data hygiene: players named "Participant" and "Billy Ballbags" in your test pools.
11. `?` (unrevealed) cells were never observed — expected, you are the commissioner on every pool visited; **the member view of #432 is still unverified** and needs a non-commissioner account.

Console: zero errors/warnings across the Pick'em, Margin and Survivor pages
after arming; the initial page loads before arming were not captured.

---

## 6. What is blocked, and exactly what unblocks it

| Item | Blocked on | Unblock |
|---|---|---|
| Items 9, 11, 12 (clickable rows to picks; week-score column; sort toggle) | **T0** must merge first (row identity = `row.id`; guard in `nfl-surface-invariants`) — otherwise they get built uid-keyed and reworked | merge #434–#437, then I open T0's PR (codex + qodo), then build 12 → 11 → 9 as separate PRs |
| Item 10 (Standings column names, "MNF Score" → tiebreaker wording; clickable Week Pick) | nothing for the rename; the clickable half is item 9 | I can ship the rename any time |
| Item 7 (menu condensation) | your taste — "How It Works" and "Pool Guides" go to the same page (`/how-it-works` vs `?view=strategy`); logged-in header has no "Features" | say "drop Pool Guides" (recommended) or tell me the target nav, and whether any of the 8 pool tabs may merge |
| Item 13 | §4 ruling | say A or B |
| Item 5 (move hybrid split control) | ledger K8 = Yes | then it is an ordinary PR with the interaction test |
| Item 6 (weekly + season prizes) | ledger K1–K2, K9 + WEEKLY-PRIZES signed | then T1–T2 |
| Item 4 (ledger) | ledger K3–K7, K10–K12 + item 6 landed | then T3–T6 |
| Item 2 (co-commissioners) | co-comm K1–K8 | T1 lock can ship on K2 alone |
| Item 3 (multi-entry) | multi K1–K11 + K9's tiny PR | T1… |

---

## 7. If you only have five minutes

1. Merge #434 (§2 step 4, first command).
2. Read §3b–§3d and reply "all recommendations" or list the K's you change.
3. Say **A** or **B** for item 13 (§4), and what item 15 was.
4. Tell me to open T0's PR.
