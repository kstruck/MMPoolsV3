## Needs sign-off

- [ ] **Create a `pr-ci-check` skill.**
  - evidence: Across ≥4 sessions (PRs #97, #98, #110, #111, #113, #114) Kevin pastes a failing GitHub Actions URL and asks Claude to check it; the fix each time is `gh run list --branch <branch> --limit 3` then `gh run view <run-id> --log-failed`, report pass/fail. Currently reinvented per-PR rather than codified — one instruction embedded inline already spells out the exact steps.
  - proposed action: add a `pr-ci-check` skill (repo-local `.claude/skills/` or global) that takes a PR number/URL, runs the gh CLI check sequence, reports pass/fail in caveman format.
  - options: approve | reject | approve and don't ask again

- [ ] **Record ticket/phase/proceed execution model + model-routing rule as project memory.**
  - evidence: Standard workflow on march-melee-pools: audit → synthesize findings into numbered tickets (T1–T14) → group into phases → execute phase-by-phase, gated on an explicit "proceed" each phase. Model routing: standard tickets go to whichever model is active, complex tickets get flagged for Opus, same-tier tickets get batched into one session. Confirmed repeatedly (07-02, 07-03) with no pushback — this is the default shape for any nontrivial multi-fix effort on this repo.
  - proposed action: add a project-type memory describing the ticket/phase/proceed-gate structure and the Opus-for-complex / batch-same-tier routing rule, so future planning sessions on this repo default to it.
  - options: approve | reject | approve and don't ask again

- [ ] **Record parallel-session worktree discipline as project memory.**
  - evidence: When running multiple Claude Code sessions on this repo concurrently, Kevin isolates work into its own git worktree and explicitly holds off starting overlapping ticket batches until another active session's work lands, to avoid conflicts. Stated as a precaution by Kevin himself (not Claude-suggested), appearing twice on 07-03.
  - proposed action: add a project-type memory — default to requesting/using a separate worktree before starting a new ticket batch if another session may be active on the same repo; ask rather than assume parallel work is safe.
  - options: approve | reject | approve and don't ask again

## More context required

- [ ] **Dual-deploy sequence (Firebase functions + Coolify frontend) via PR merge.**
  - what's unclear: This exact sequence (push branch → PR → merge to main → deploy Firebase functions if backend touched → deploy frontend via Coolify) recurred without correction across 07-02/07-03 sessions — but is it already documented in the repo's own README/CLAUDE.md? If so, this is a duplicate and shouldn't become a separate memory entry.
