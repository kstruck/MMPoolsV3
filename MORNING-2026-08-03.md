# MORNING — 2026-08-03

**No production action is pending, and nothing is waiting on you to unblock it.**
Tasks 1 and 2 shipped, deployed and were verified from outside; §1 is evidence,
not a runbook.

⚠️ **Work WAS deferred, though**, so this is not "nothing left to do":
**Task 3 — `PLAN-IMPORTER-SAFETY.md` and the worktree pruning — was not
started**, for time. §5 and the §7 prompt carry it forward. Nothing about it is
urgent or pilot-blocking.

**3 days to the HOF game (Thu 2026-08-06, 8:00pm ET, CAR at ARI.)**

> This is the ONLY morning doc for 2026-08-03. If a second one ever appears for
> this date, `tests/docs-state-invariants.test.ts` now FAILS unless one of them
> names the other in its first ten lines — see §3.

---

## 0. The one thing worth reading first

**Production holds ZERO forged Member Records.** You asked me to tell you the
count because it decides whether you bother with the cleanup sweep. I measured
it, read-only, from the Firebase console:

- collection-group query on `members`, limit 1000, **156 documents total**;
- enumerated all 156 across four pages;
- **every one carries `joinedAt`** — zero without it;
- `memberReportedPaid` / `memberReportedAt` do not appear in the result set at
  all, which is consistent with the plan's claim that the self-report has never
  had a client caller.

**So `PLAN-SETPAIDSTATUS-MEMBERSHIP` §7's prod-data sweep has nothing to clean
and does not need running.** I did not write anything; this was reads only.

⚠️ **Caveat, stated rather than buried.** This is a point-in-time count of what
is in the collection now, and it proves the exploit was never used *and left a
record behind*. It does not prove nobody ever called the endpoint. #347 is still
worth having: it is the guard against a record forged in future, and it cost
nothing to keep.

---

## 1. State — verified, not remembered

| Thing | State |
|---|---|
| Deployed application source | `0f548bf` (#347) |
| `origin/main` tip | ahead of that after the docs merges. **Docs-only commits do not change what is deployed** — `git fetch origin` FIRST, then `git diff --name-only 0f548bf..origin/main` (ALL paths, unfiltered) before assuming a deploy is owed. ⚠️ Do not narrow that to `functions/ shared/ firestore.rules src/`: `firebase.json` and `firestore.indexes.json` are deploy inputs too, and a change touching only one of them would leave the narrowed diff empty — #334 was exactly that |
| Functions | **deployed from `0f548bf`** — certified by a FOURTH run: 173 all `Skipped (No changes detected)`, 0 updates, 0 failures, `✔ Deploy complete!` |
| Rules | **≡ `0a705c0`**, unchanged |
| Frontend bundle | `index-DlH8liQe.js` → **`index-H9HjG31q.js`**, read off the prod HTML |
| Open PRs | dependabot #299–#304 (untouched, still rejected), **plus [#348](https://github.com/kstruck/MMPoolsV3/pull/348) which is YOURS** — see below — and #350, this doc |
| Test baselines at `0f548bf` + #349 | **measured 2026-08-03** (not copied forward): functions **1295**, root **574**, emulator **306** passed / 2 expected fail / 10 skipped. Re-measure rather than trusting these — root moved twice today (523 → 531 → 574) as #347 and #349 landed |

**ALL THREE QUEUES EMPTY.**

### Merged

| PR | What |
|---|---|
| [#347](https://github.com/kstruck/MMPoolsV3/pull/347) → `0f548bf` | A forged Member Record no longer shows on the commissioner roster, in `memberCount`, or in the dues totals. |
| [#349](https://github.com/kstruck/MMPoolsV3/pull/349) → `a6b7cf4` | The docs-state guard: a dated state heading that its own section has moved past, plus same-date MORNING docs that do not name each other. |

### ⚠️ [#348](https://github.com/kstruck/MMPoolsV3/pull/348) appeared mid-session and is NOT mine

Your brief said the only open PRs were dependabot #299–#304. **#348 —
*"feat(ops): Production Watchdog — the last 24h of real user activity"*, branch
`feat/prod-watchdog` — was opened at 04:19 UTC while I was working, authored by
`kstruck`.** I did not touch it, review it, or merge it: my autonomy grant covers
merging *my own* PRs.

What you need to know about it:

- **It does not affect anything above.** It is unmerged, so `origin/main` and
  every deploy claim in this document are unchanged by it. I verified there is
  **zero file overlap** with #347/#349, so there is no clobber risk in either
  direction.
- **Its CI is green** (7/7 at the time of writing).
- **If you merge it, it owes BOTH a functions deploy and a Coolify rebuild** — it
  adds `functions/src/prodWatchdog.ts`, exports it from `functions/src/index.ts`,
  and adds `src/components/admin/ProductionWatchdogCard.tsx`.
- ⚠️ **It changes `functions/src/index.ts` exports, which `mmp-change-control` §1
  lists as an AUTHORIZATION trigger** — i.e. the plan gate applies unless the
  export changes nothing about who may do what. Your call, not mine; flagging it
  because the classification is not obvious from the diff alone.
- It also edits `.claude/skills/mmp-qodo-cycle/SKILL.md`, so another session was
  writing to the skills tree at the same time as this one.

### The deploy took four runs, and only the fourth counts

Runs 1–3 each hit HTTP 429 (`Per project mutation requests per minute per
region`). **Run 3 completed the whole fleet** — `✔ Deploy complete!`, 153 skipped
+ 20 updated — **and was still not the certification**, because a run reporting
updates is not evidence that the deployed code matches. Run 4 was 173 all-Skipped
with zero updates. That is the run I am claiming.

---

## 2. #347 — the roster fix, and the finding that changed its design

`#344` stopped `setPaidStatus` minting a Member Record in any pool; `#338` stopped
an already-forged one being a *reminder target*. Neither deletes what was already
minted, and §4a covered targeting **only** — so `src/utils/poolRoster.ts` still
put every document in the members collection on the commissioner's roster, in
`memberCount`, and in the dues totals.

Three readers in that file walk the collection, so I routed all three through one
helper rather than fixing the visible one.

### 🔴 The finding worth reading

I first filtered on the canonical `joinedAt` stamp alone, matching #338. **codex
round 1 showed that also discards a real participant's un-stamped record** — and
those carry real money state:

- `setPaidStatus`'s **commissioner branch merges `paidStatus: 'PAID'` without
  stamping `joinedAt`**;
- its own comments record that `reconcilePaymentTruth` promotes claim-only
  documents to PAID from a paid entry.

The failure would have been silent and expensive: **you mark someone paid, the
roster keeps showing UNPAID, and `collected` loses the fee.** Hiding payment
truth in order to hide a forgery. I verified this against the source before
acting on it rather than taking the reviewer's word.

The fix uses `isProvableMember` — canonical record **OR** `participantIds` — which
is the rule `PLAN-SETPAIDSTATUS-MEMBERSHIP` §4 already endorses. The #344 exploit
wrote *only* the member document, so a forged record still has neither evidence
and still vanishes. That predicate **moved to `shared/memberRecord.ts`** (re-exported
from the functions-side file, so no importer changed): `src/` cannot import that
file because it pulls in `firebase-admin`, and re-deriving the two-evidence rule
client-side is the drift that hoisting the first predicate existed to prevent.

This door is deliberately **looser** than the reminder door, which still uses the
canonical half alone. #338 refuses `participantIds` there because it is
manager-writable and would make that callable an arbitrary-email primitive.
Nothing on the roster path sends anything — the consequence is a row on the
manager's own roster and a fee in their own dues total.

**One reader is deliberately NOT filtered**, and it is commented in place so it
does not read as an oversight: `PaymentsPanel`'s `myMember` is keyed on the
caller's own uid, so the only record it can return is their own.

---

## 3. #349 — the docs-state guard you asked for two sessions ago

The obvious rule — *heading date must not be older than the newest date in its
section* — was already measured as crying wolf on three real headings. **I
re-measured and it also fires on `PICKUP §0`**, whose section mentions
`2026-08-13`, the first 16-game slate: a future deadline, not a record. Two
independent false-positive classes, and this repo has twice written down that an
invariant which cries wolf gets ignored and then the real one is missed.

So the rule is narrowed on both sides: the **heading** must carry a state word
from a closed vocabulary, and the offending **content** date must sit on a line
that also carries one — same kind of fact, not merely a later number.

**Measured, and re-measured after every subsequent narrowing:**

- **0 fires** on the corpus as it stands;
- **exactly 1 fire** against `PICKUP-PRESEASON-PILOT.md` as it was at `3cfd968^` —
  the real defect — on the right heading, citing the right line.

The second case fails when two `MORNING-<same-date>*.md` files exist and neither
names the other in its first ten lines. Its vocabulary includes `continues`, not
only `supersedes`, because two of the four real same-date pairs are genuine
continuations; forcing "superseded" onto those would mean writing something false
to satisfy a test. Three historical docs gained an accurate banner, with the
creation order taken from `git log --diff-filter=A` rather than guessed from
filenames.

### ✅ It caught a live instance the same hour, and the instance was mine

While updating the deploy-state docs for this handoff, the guard failed on:

```
PICKUP-PRESEASON-PILOT.md:336 heading says 2026-08-02 —
"## 2. Live state (deploy state verified 2026-08-02)" —
but its section says 2026-08-03: "Frontend rebuilt 2026-08-03 04:01 UTC..."
```

That is the **same heading**, in the **same file**, in the **same failure mode**
that codex had to find by hand two sessions ago — re-introduced by me twenty
minutes after I shipped the test for it, and caught mechanically this time. That
is the whole argument for the test, and I did not have to construct it.

---

## 4. Reviews — what the two reviewers actually caught

**codex: 9 findings across the two PRs, every one valid, every one absorbed.**
The pattern this repo has recorded held again — round 1 finds defects in the
code, and rounds 2+ find defects in the fixes. On #349 rounds 2, 3, 4 and 5 each
found a hole **in the previous round's fix**:

| # | What |
|---|---|
| #347 r1 | P1 — canonical-only filter would hide the commissioner's own PAID mark |
| #349 r1 | P2 — `max` over the heading's dates masked staleness via a future date |
| #349 r2 | P2 — the exact mirror on the body side, crying wolf on a game date |
| #349 r3 | P2 — "first date after the phrase" fails when the claim date precedes it |
| #349 r4 | P2 ×3 — relationship word unbound from the sibling name; a blanket `superseded` substring skip discarding a CURRENT update; and **my banner claiming two-way discovery the paired file did not deliver** |
| #349 r5 | **P1 — I introduced a lint ERROR** and read past it |

⚠️ **The lint error is the one I want on the record.** My own gate output printed
`1133 problems (1 error, 1132 warnings)` and I read the line as passing. Running
a gate is not the same as reading it. `⚠️` inside a regex character class trips
`no-misleading-character-class`; it is fixed and lint is back to 0 errors.

**qodo: 0 findings on both PRs**, and on both it independently endorsed the
approach — on #347 it rejected the canonical-only alternative for the same reason
codex did, before I told it anything. Its one forward-looking note on #349 was to
extract the scanners into a module *"if additional doc rules accumulate"*. Not
acted on: that is speculative until a third rule exists.

**Mutation testing: 18 mutants on #349, 9 on #347, all killed.** Each was
anchor-asserted for uniqueness before being applied and the tree committed first.
Two of them (N9, N10) are the guard-about-a-guard case you asked for — making
each scanner structurally incapable of reporting fails 4 and 5 tests. **One
mutant produced a syntax error rather than a result** and was re-run properly
before being counted, per the rule that a mis-targeted mutant is not a result.

---

## 5. Untouched, on purpose

- **Dependabot #299–#304** — all six still open, still rejected, not touched.
- **App Check** — `VITE_RECAPTCHA_SITE_KEY` not set. I never opened the Coolify
  environment screen. The warning is the safe state.
- **`SECURITY-CLAIM-SQUARES.md`** — the squares-theft hole stays open by your
  standing decision (HANDOFF §5). #347 does not touch it.
- **`system/config.nflAutoScore` and `statsRecompute`** — still unset, still
  disabled. Not armed.
- **The rescore-queue path and `nflDeepSweep`** — still yours, not armed.
- **No production data was written.** The only prod interaction beyond the deploy
  was the read-only count in §0.
- **Your Task 3 — `PLAN-IMPORTER-SAFETY.md` (docs only) and the worktree pruning,
  which is `MORNING-2026-07-30.md` **Task 2** — not started; your Tasks 1 and 2
  plus their nine review rounds took the session. (Two different numbering
  schemes meet here: it is the third item in *your* queue and Task **2** in that
  older doc. The procedure to follow is the one in `MORNING-2026-07-30.md`.)

---

## 6. Residuals I am carrying, stated rather than buried

1. **A Firestore index was NOT created.** The direct query for the forged-record
   count (`memberReportedAt > 0` over the `members` collection group) needs a
   `COLLECTION_GROUP_ASC` index that does not exist. Creating one is a production
   configuration change, not a read, so I enumerated all 156 documents instead
   and reached the same answer. If you ever want that query to be one click, the
   index is the thing to add — but nothing needs it today.
2. **The docs-state guard compares DATES, not claims.** A heading saying "queue
   EMPTY" above content listing owed work still passes. Automating that needs a
   canonical form for queue prose, which is the same reason the SHA guard has
   never compared it.
3. **`MORNING-2026-07-25` and `MORNING-2026-08-01` pairs are continuations, not
   supersessions**, and are now labelled as such. If you disagree with either
   characterisation the banner is one line to change; I took the relationship
   from what each doc says about itself, not from its filename.

---

## 7. Copy-paste prompt for the next session

```
Continue March Melee Pools. Repo D:\march-melee-pools — work in fresh worktree
branches off origin/main.

⏰ HOF GAME 2026-08-06 (Thu 8:00pm ET, CAR at ARI). THREE DAYS. First 16-game
slate 2026-08-13. Prioritise by "does this break the pilot".

READ FIRST: CLAUDE.md, HANDOFF.md STOP POINT box, PICKUP-PRESEASON-PILOT.md §0,
MORNING-2026-08-03.md.

STATE — VERIFY, DO NOT TRUST (these are BASH, for the agent's own shell):
  git fetch origin          # FIRST - origin/main is a stale local ref otherwise
  git log --oneline -3 origin/main
  gh pr list
  # EVERY deploy input, not just the obvious three. firebase.json and
  # firestore.indexes.json are deploy inputs too, and a change touching ONLY one
  # of them leaves a narrowed diff empty and reads as docs-only. List all changed
  # paths; the mapping below says what each one owes.
  git diff --name-only 0f548bf..origin/main
  curl -sSL https://www.marchmeleepools.com/ | grep -o 'index-[A-Za-z0-9_-]*\.js'

Kevin, PowerShell 5.1 form of that last one — plain `curl` is Invoke-WebRequest
and rejects -sSL, and `grep` does not exist:
  (curl.exe -sSL https://www.marchmeleepools.com/ | Select-String -Pattern 'index-[A-Za-z0-9_-]+\.js' -AllMatches).Matches.Value | Select-Object -First 1

DEPLOYED APPLICATION SOURCE is 0f548bf (#347). Functions certified by a FOURTH
run, 173 all-Skipped. Rules ≡ 0a705c0. Bundle index-H9HjG31q.js. ALL THREE
QUEUES EMPTY unless a merge since touched a deploy input:
  functions  -> functions/**, shared/**
  rules      -> firestore.rules
  indexes    -> firestore.indexes.json (SEPARATE from rules — deploying rules
                does NOT create an index)
  firebase.json -> ROUTE IT BY SECTION, it feeds all three. Its `functions`
                block carries the `ignore`/`predeploy` config and changes the
                UPLOAD; its `firestore.rules` and `firestore.indexes` keys point
                at which files get deployed. A change to any of those owes the
                corresponding deploy, and listing it under `functions` alone —
                as an earlier draft of this handoff did — would deploy functions
                and leave prod on the previous rules or indexes.
                If you cannot tell which section moved, deploy all three
                (functions BEFORE rules): they are idempotent, and the
                all-Skipped run is the evidence either way.
  Coolify    -> DEFAULT YES; a merge owes a rebuild unless everything it touched
                is under functions/, firestore.*, firebase.json, .claude/**,
                .github/**, docs (*.md), or tests/. shared/** owes BOTH.
                firebase.json is on that no-rebuild list because prod www is
                nginx/Coolify, NOT Firebase Hosting — nothing in the frontend
                image reads it. #334 is the precedent: it changed firebase.json
                (plus functions/** and tests/), touched no src/, and correctly
                owed no rebuild.
                ⚠️ This exception list has been holed three times by finding
                one more file the build actually reads (shared/**, public/**,
                index.html, scripts/prerender.ts, tailwind.config.js,
                postcss.config.js). If a change is not PLAINLY inside the list,
                REBUILD: an unnecessary rebuild costs two minutes, a missed one
                ships nothing to users.
Baselines at 0f548bf+#349, MEASURED 2026-08-03 (re-measure, do not trust — root
moved 523 -> 531 -> 574 during that session): functions 1295, root 574, emulator
306 passed / 2 expected fail / 10 skipped.
Coolify dashboard URL — find it by SEARCHING, never
by line number (anchors rot). Bash:
  grep -n "Dashboard, for the next time" HANDOFF.md
Kevin, PowerShell 5.1:
  Select-String -Path HANDOFF.md -Pattern "Dashboard, for the next time"

⛔ Dependabot #299–#304: do NOT merge, all six rejected with evidence.
⛔ PR #348 (feat/prod-watchdog) is KEVIN'S, opened 2026-08-03 mid-session. Do NOT
   merge it. If Kevin merges it, it owes BOTH a functions deploy and a Coolify
   rebuild, and it touches functions/src/index.ts exports (an authorization
   trigger under mmp-change-control §1).
⛔ APP CHECK: do NOT set VITE_RECAPTCHA_SITE_KEY. The warning is the safe state.
⛔ SQUARES: SECURITY-CLAIM-SQUARES.md is a KNOWN hole, accepted through the
   pilot by standing decision (HANDOFF §5). Do NOT start it.
⛔ system/config.nflAutoScore + statsRecompute UNSET = disabled. Leave them.
⛔ The rescore-queue path and nflDeepSweep are Kevin's. Do not arm either.
✅ The PLAN-SETPAIDSTATUS-MEMBERSHIP §7 cleanup sweep is CLOSED — prod was
   measured 2026-08-03 to hold ZERO forged Member Records (all 156 carry
   joinedAt). Do not re-derive this; do not run the sweep.

QODO IS ON — check all three surfaces on every PR. If a push does not trigger a
re-review, advance the watcher's SINCE FIRST and then toggle draft→ready.
⚠️ The SINCE line is BASH (for the agent's own shell), not PowerShell 5.1 —
`$(date -u -d ...)` is a parse error in PowerShell. Agent, bash:
  SINCE=$(date -u -d '1 second ago' +%Y-%m-%dT%H:%M:%SZ)
Kevin, PowerShell 5.1 equivalent if you ever run it yourself — one per line:
  $SINCE = (Get-Date).ToUniversalTime().AddSeconds(-1).ToString("yyyy-MM-ddTHH:mm:ssZ")
Then, either shell:
  gh pr ready N --undo
  gh pr ready N
A timeout after that is "qodo did not review", never "clean".

AUTONOMY: merge your own PRs when 7/7 CI + codex clean + qodo resolved + your
read agrees. Deploy functions when needed (npm --prefix functions ci, NOT
install); expect HTTP 429 partway through a full-fleet run and re-run until a
pass reports 173 all-Skipped with ZERO updates — a run that completes the fleet
but still reports updates is NOT the certification. No prod-data writes, no
arming jobs, no dependabot, no Coolify env-var changes.

TASK: PLAN-IMPORTER-SAFETY.md (docs only; ⛔ NO re-import, not even dry-run),
then worktree pruning per MORNING-2026-07-30 Task 2. Both were deferred from
2026-08-03 for time.

RULES: gates per PR (functions build, functions typecheck — a REQUIRED CI job
that `npm run build` does NOT cover — functions test, emulator, tsc -b, npm
test, lint 0 errors). ⚠️ READ the lint total, do not just run it: "1 error" hid
in a 1133-line summary and codex caught it, not me. codex exec review --base
origin/main, three-dot scope check, -b never -B, up to 10 rounds. MUTATION-TEST
EVERY GUARD and ANCHOR the mutant; a mutant that produces a syntax error is not
a result — re-run it. COMMIT BEFORE MUTATION LOOPS. Known flake:
opsAlertDispatcher.test.ts.
```
