# MORNING 2026-07-30 — overnight takeover

Seven days to the Hall of Fame game (2026-08-06, Thu 8:00pm ET). **Two PRs from
your queue shipped and are deployed** — A (#322) and B (#324) — plus two docs
PRs. C, D and E are not started, and §5 says why.

---

## 1. Deploy state — READ THIS FIRST

| Queue | State | Evidence |
|---|---|---|
| **Functions** | **EMPTY** | `git diff --name-only 6676580^ 6676580 -- functions/ shared/ firestore.rules firestore.indexes.json` returns **zero files** — run it yourself, it works from `main` today. #322 touched nine files and none of them is relevant to the FUNCTIONS or RULES queues. (**Four** of them ARE emitted `src/**` code, which is why the frontend row below is a different story — "no backend deploy" is not "no deploy". The fifth `src/**` path is `poolRoster.test.ts`, which only Vitest loads and Vite never emits.) Nothing was deployed to functions, nothing is owed; the deployed SHA is still the tagged claim in HANDOFF's STOP POINT box. |
| **Rules** | **EMPTY** | Same command, same empty result — `firestore.rules` is not in #322's file list. |
| **Frontend** | **EMPTY — I rebuilt it TWICE and verified both** | #322: `index-D1wLGiMy.js` → **`index-C31xivRN.js`**. #324: `index-C31xivRN.js` → **`index-CR5oJEHh.js`**. Both read straight off `https://www.marchmeleepools.com/`. |

⚠️ **One correction to a rule in your own brief, because it will misfire one
day.** You wrote that the bundle hash "MUST change for a `src/**` change". It did
here, and it is the right check to reach for first — but it is not universal.
Vite hashes the **emitted** bundle, so a `src/**` change confined to test files,
or to code the tree-shake drops, emits byte-identical assets and leaves the hash
alone. #322 itself contains such a file. A moved hash is positive proof the
rebuild shipped; an unmoved one is **inconclusive**, not a failure — read the
Coolify deployment log in that case. Written into HANDOFF's STOP POINT box so the
next session inherits the nuance rather than condemning a good deploy.

**I triggered two Coolify rebuilds.** Those are the only outward actions I took.
I deployed no functions and no rules, mutated no production data, armed no
scheduled job, merged no dependabot PR, and changed no Coolify environment
variable.

`system/config.nflAutoScore` and `statsRecompute` are still UNSET. I did not
touch them.

### What is on `origin/main` now

```
245f3d4  refactor(commissioner): split the manager page into four sections (#324)
ead73ff  docs: MORNING-2026-07-30 (#323)
6676580  fix(commissioner): the Buy-In Ledger was blind to every member without an entry (#322)
20fae26  fix(routing): /admin/:id dead-ended on every NFL pool (#321)
```

⚠️ **`origin/main`, not `main`.** As I write this your local `main` is still
`20fae26` — `git rev-parse --short main` says `20fae26` while `origin/main` is
four commits ahead — because local `main` only advances when someone runs
`git pull` in the main checkout, and I worked entirely in worktrees. This is the
trap CLAUDE.md §2c already names. Run `git fetch origin` and branch off
`origin/main`, or you will cut work off a tree that has none of tonight's work in
it.

---

## 2. PR #322 — the commissioner Buy-In Ledger, and four cards that reported things that never happened

**Merged `6676580`. Deployed and verified live.**
<https://github.com/kstruck/MMPoolsV3/pull/322>

### What it fixed

You found the Buy-In Ledger reporting `$0` projected, `$0` collected, `0%`
clearing rate and *"no members matching filter criteria"* — beside a Member
Roster panel, **on the same page**, listing those members correctly.

**Root cause.** Every figure on the card and in the Advanced Payment Ledger modal
was `entries`-derived. No write can fix that: `setPaidStatus` mirrors display
fields onto the entry only `if (entrySnap.exists)`, so an entry-backed **reader**
cannot see a member who never submitted one. That is the half of D13 that P1
could not reach.

Two more money figures were wrong without needing an empty roster:

- the fee defaulted to `entryFee || 20`, so a **free pool projected $20 a head**
  nobody owed;
- Clearing Rate divided by entry holders, so a pool where one of four members had
  paid — and only that member had an entry — read **100%**.

### How it was fixed

There were already **three** derivations of this data: the roster merge in
`NFLManagerView`, the dues maths in `PaymentsPanel` (hardened over four codex
rounds under PLAN-PAYMENT-TRUTH P3), and this card's entries-only version. The
disagreement **was** the multiplicity, so the first two moved into
`src/utils/poolRoster.ts` unchanged — comments included — and all three surfaces
now read from there. Same one-definition fix as #315, #319 and HANDOFF item 11.

### The honesty pass found four more fabrications nobody had reported

Extending the T3 "no fake dashboard cards" invariant to this file — it had only
ever covered `SuperAdminBentoDashboard.tsx`, which is exactly why this defect
class survived untouched on the commissioner one — turned up:

1. **Two commissioner buttons that did nothing.** One popped a toast announcing
   an ESPN score recalculation had begun and called NOTHING; the other did the
   same for locks. The real control is "Score & Recap Week N" further down the
   same page. **This was the most dangerous of the set** — seven days out, a
   commissioner clicking the decoy would have been told the scores were
   recalculating when nothing had happened.
2. **An invented audit trail.** The "League operations log" rendered hardcoded
   relative timestamps against a standings finalization and an ESPN schedule
   sync, neither of which had run on the pool. The genuine equivalent already
   exists and is already reachable — the Payments tab renders the append-only
   `pools/{id}/payments` ledger with real timestamps, for the commissioner too —
   so that card is **removed** rather than rebuilt as a second reader.
3. **A hardcoded sixteen-hour pick deadline**, rendered on every pool whether or
   not the week held a game.
4. **A footer claiming an active AI moderation capability** that does not exist.

Plus the two you named: the banter feed's invented scouting report about a
leader's late-season collapse (the top-player fallback on an empty pool was a
mock name lifted from `DevDashboardPreview.tsx`), and the chart's **negative**
left margin. The margin alone was not the whole fix — without an explicit `YAxis`
width recharts falls back to 60px and re-crops the same labels.

### Plan gate

⚖️ **Money surface → plan-gated.** Per your 2026-07-28 ruling it is recorded as
**PLAN-PAYMENT-TRUTH §6b**, not a new `PLAN-*.md`.

### Codex — 6 rounds, 7 findings, all valid, all absorbed, **none rejected**

**Rounds 2, 4 and 5 each found a defect in the PREVIOUS round's fix.** That is
the pattern CLAUDE.md §2c predicts, and it is why a clean round 1 is not the
review.

| Round | Result |
|---|---|
| 1 | **3 × P2.** The replacement deadline showed the first KICKOFF, not the lock the server enforces; the green all-clear could sit above a positive Outstanding Due; the head count was a `Math.max` of three source sizes rather than their union. |
| 2 | **2.** Round 1's own fix stopped counting an entry-only member's payment (a regression it introduced); and one week deadline is only true for weekly-hard-lock pools, because default Pick'em is `PER_GAME`. |
| 3 | **CLEAN.** Self-review then found the card can now list someone `setPaidStatus` rejects, behind copy blaming "Insufficient permissions or network loss". |
| 4 | **1 × P3.** Round 3's fix still misreported it: `getUserMessage` resolves the transport CODE (`functions/not-found`) before it ever looks at the message, and `setPaidStatus` uses that code for both "pool not found" and "member not on roster". |
| 5 | **1 × P2.** "Unpaid" was a payment STATUS, not a DEBT — so a seeded owner (`feeOwed: 0`) and every member of a free pool sat in the owing queue with a meaningless "Mark Paid", beside tiles reading Expected $0. |
| 6 | **CLEAN**, and my own read of the diff agrees. |

**Two findings came from self-review, not codex.** The round 5 fix made the
debt-filtered list contain rebuy-only debtors — who are `paidStatus: 'PAID'` — so
the row's "Mark Paid" button would have set them **UNPAID**, on a money surface.
And that same change made round 1's separate rebuy empty state unreachable, so it
was deleted rather than left standing as a branch that looks like a safeguard and
can never run.

### Mutation testing — one guard did NOT hold

**38 mutations applied. 37 killed on the first attempt. One SURVIVED.**

The guard written for the round 4 fix pinned the plumbing — the parameter, both
call sites, the arity — every one of which survives reverting the function body
to a bare `getUserMessage(err, fallback)`. It looked like it guarded the fix and
guarded nothing. It now pins the branch, and a mutation collapsing both sides of
the ternary fails it. **The mutation caught it; reading it did not.** This is the
fourth time in this repo a test has looked like it guarded and did not.

### Gates

| Gate | Result | Baseline |
|---|---|---|
| `npm --prefix functions run build` | pass | — |
| `npm --prefix functions test` | **1170 pass** | 1170 |
| `npm --prefix functions run test:emulator` | **250 pass / 2 expected fail / 10 skipped** | identical |
| `npx tsc -b` | pass | — |
| `npm test` | **436 pass** | 382 (+54 new) |
| `npm run lint` | **0 errors**, 1110 warnings | ~1104 warnings |
| `npm audit --omit=dev` (root) | 0 vulnerabilities | — |
| `npm audit --omit=dev` (functions) | 1 moderate — `protobufjs` | pre-existing, already tracked |
| CI on the PR | all 7 checks pass | — |

### NOT verified

**Nobody has clicked this on marchmeleepools.com.** The behaviour is covered by
29 unit cases and the wiring by invariants, and the bundle is confirmed live, but
the card itself has not been exercised against a real pool in production. That is
task 1 below.

### Deliberately left for the next PR that touches `functions/`

`shared/` is compiled into `functions/` by the predeploy hook, so touching it
would have owed a full functions deploy for a change with no runtime effect.
Three items are parked, all recorded in PLAN-PAYMENT-TRUTH §6b:

- `MemberRecord` does not declare `paymentMethod` / `paymentNote`, though
  `setPaidStatus` writes both onto the record.
- `setPaidStatus.ts` still justifies its entry mirror by "the Bento ledger UI is
  entry-backed". It no longer is. **The mirror itself is still correct and still
  wanted** — `reconcilePaymentTruth` depends on the two stores converging — only
  the stated reason is stale.
- Its `not-found` error should carry a domain prefix (`getUserMessage` already
  resolves `/^[A-Z_]{4,}:/` ahead of the transport code). The client-side
  workaround shipped instead.

---

## 3. Findings retracted, and what reviewing THIS document found

**No product finding retracted.** I did not overturn any earlier claim, and I
made no file:line assertion I did not verify at the byte level first.

**This document went through nine codex rounds of its own, and produced the
single most serious finding of the session.** Ten findings, all valid, none
rejected. It is worth listing because eight of the ten were defects in *my own
corrections* rather than in the original text:

| Round | What it caught |
|---|---|
| 1 | HANDOFF — the canonical live-state carrier — still told the next operator a rebuild was owed that I had already run. Exactly the contradiction `tests/docs-state-invariants.test.ts` deliberately does NOT automate: it compares deploy SHAs, not queue prose. |
| 2 | "The hash MUST change for a `src/**` change" is not universal — see the box in §1. |
| 3 | My evidence command was vacuous. I quoted `git diff origin/main...HEAD` as proof #322 owed no functions deploy; correct when I ran it pre-merge, meaningless quoted in a doc read after the merge. |
| 4 | Both of round 3's replacement claims overreached — "none deploy-relevant" conflated *no backend deploy* with *no deploy*, and "three-dot is vacuous after merge" is false in a squash-merge repo. |
| 5 | I said five emitted `src/**` files; four are emitted, because `poolRoster.test.ts` is Vitest-only — contradicting the hash warning two paragraphs above it. |
| 6 | I fixed that count in HANDOFF and left it wrong in this doc (a sweep miss), and headed the deploy snapshot "what is on `main`" when local `main` was stale at `20fae26`. |
| 7 | I named two orphaned worktrees; there are six. |
| 8 | **P1 — my own task list would have destroyed the only copy of `PLAN-TRUST-BOUNDARY.md`.** See the box in task 2. Plus a PowerShell inspection command that did not prune `node_modules` and so would have fed a delete decision the wrong answer. |
| 9 | CLEAN, and my own read agrees. |

The pattern is the same one PR #322 showed and CLAUDE.md §2c predicts: **a fix is
not safer than the thing it fixes.** Rounds 4, 5 and 6 exist purely because round
3 tried to correct round 2's wording and got it wrong twice.

---

## 3b. PR #324 — the tabbed commissioner split

**Merged `245f3d4`. Rebuilt and verified live** — bundle `index-C31xivRN.js` → **`index-CR5oJEHh.js`**.
<https://github.com/kstruck/MMPoolsV3/pull/324>

This is the PR the first draft of §5 said I had not started. I went on and did it.

`NFLManagerView` was one ~870-line scroll, and that length is the *reason* the
same `SaveSettingsControl` appeared five times — HANDOFF item 3 calls them
harmless and deliberate, which they were, but they exist only because the save
button was otherwise nowhere near whatever you had just edited. Four sections
now: **Overview** (bento, payouts card, control-room header), **Members &
Payments** (the roster), **Scoring** (Score & Recap), **Settings** (rules editor
plus the exceptions block). Four of the five save buttons are gone; the one at the
foot of Settings remains.

Two things I decided rather than defaulted:

- **The feedback alert sits outside every tab group.** A save started on Settings
  must still report its result if the tab changed underneath it.
- **State is local, not in the URL.** `activeTab` already rides in the URL for the
  pool page — that is why `AdminRoute` redirects NFL pools to `?tab=manager` — and
  a second URL-backed tab would give the surface two sources of truth.

The three-column grid that made the roster share a row with the scoring console is
dissolved; each is full width in its own section.

**Codex: 2 rounds, 1 finding, valid, absorbed.** The nav claimed
`role="tablist"`/`role="tab"` without implementing the pattern — those roles are a
promise of keyboard behaviour (Arrow/Home/End under a roving tabindex, associated
`role="tabpanel"`) that the browser does not supply. Announcing a tablist and then
ignoring the arrow keys is worse for a screen-reader user than plain buttons. They
are now ordinary buttons in a `<nav aria-label>` with `aria-current="page"`.
Round 2 clean.

**Mutations: 12 applied, 12 killed.** One did not hold first time — the
feedback-alert guard compared `indexOf` positions without first asserting the
block exists, and deleting it returns `-1`, which is still "less than". **That is
the second guard in two PRs tonight to pin position without pinning existence.**

Gates: `tsc` pass · root vitest **456** · functions **1170** · emulator **250 / 2
expected fail / 10 skipped** · lint **0 errors** · all 7 CI checks green.

### ⚠️ NOT VISUALLY VERIFIED — this is task 1b, and it is the real gap

**A pure layout change that nobody has looked at in a browser.** I made two
attempts and both failed for reasons that had nothing to do with the change:

1. The preview tooling started a dev server against the **wrong directory**. Not
   assumed — measured:
   `curl -s localhost:5174/src/components/NFLPoolDashboard/NFLManagerView.tsx | grep -c COMMISH_TABS` returned **0**, i.e. it was serving code without this
   change in it. Clicking through would have shown me the *old* layout and told
   me nothing. This is the "preview MCP serves WRONG dir" gotcha already in the
   e2e notes; what the notes do not say is that it resolves `launch.json` from the
   **session's** worktree, which is why it picks the wrong root.
2. Pointing it at the right worktree worked — same curl returned **2** — but the
   app never mounted: `#root` stayed empty with no console errors and no server
   errors. That is an environment issue in the dev harness, not something this PR
   introduced, and I stopped rather than burn more time on it.

So the structure is verified — 36 invariants, 12 mutations, control placement
pinned by source **offset**, `tsc`, 456 tests — and the **appearance is not.**
Clicking all four sections once is about a minute of work and it is the only thing
between this and "verified".

I did correct one of my own commands mid-session, which is worth recording
because it will bite again:

**`git merge-base --is-ancestor <branch> origin/main` reports MERGED PRs as
UNMERGED.** It said `claude/admin-route-nfl-branch`, `claude/csp-sentry-connect-src`
and `claude/manager-seasontype-filter` were unmerged. All three are #321, #320 and
#319 — merged. The repo **squash-merges**, which rewrites the commit, so the
branch tip is never an ancestor of `main`. Verified the right way instead:

```
gh pr list --head <branch> --state all --json number,state
```

I pruned worktrees only after that command said MERGED. Had I trusted
`--is-ancestor`, I would have kept eleven stale worktrees and possibly concluded
three shipped PRs had not landed.

---

## 4. Your task list

Six tasks — 1, 1b, 2, 3, 4, 5. **Tasks 1 and 1b are the only ones I would call
urgent**, and together they are about seven minutes in one browser session.

### Task 1 — Confirm the Buy-In Ledger is right on a real pool (5 minutes)

This is one of two things I could not verify. Everything below task 1b is
optional.

1. Open a browser and go to **<https://www.marchmeleepools.com/>**.
2. Sign in as yourself.
3. Navigate to the **Pick'em pool you were working on 2026-07-29** — the one
   that showed `$0` and "no members matching filter criteria".
4. Open its **Commissioner** tab (the cog, or the `manager` tab on the pool page).
5. You will land on a **new section nav** — Overview / Members & Payments /
   Scoring / Settings. Stay on **Overview** and look at the **Buy-In Ledger**
   card.

**What you should see when it works:**

- **Projected Pot** shows the real total, not `$0` — for a pool with an entry fee
  it is roughly `fee × number of members`.
- The bar chart's two labels, **Collected** and **Outstanding**, are fully
  visible on the left-hand side and not cut off at the edge.
- The list underneath is headed **"Members Still Owing (N)"** and actually names
  people, instead of showing the green "all buy-ins cleared" banner on a pool
  where nobody has paid.
- The **AI Commissioner Chat** card's feed is **empty**, with the line
  *"Nothing posted yet…"*. There is no mention of any player leading or
  collapsing.
- There is **no fourth card** titled "Commissioner Actions" with Recalculate
  Scores / Toggle Locks buttons. That card is gone on purpose.

6. Click **View Full Ledger** on that same card.

**What you should see:** the four tiles across the top — Total Projected, Total
Collected, Outstanding Due, Clearing Rate — carry real numbers, and the table
below lists every member, including any who have never submitted a pick.

**If you do NOT see this:** you are probably on a cached bundle. Hard-refresh
with **Ctrl+Shift+R**. To confirm which bundle you have, press **F12**, open the
**Network** tab, refresh, and look for a file named `index-….js` — it must read
**`index-CR5oJEHh.js`**. If it reads anything else — `index-C31xivRN.js` or
`index-D1wLGiMy.js` — the browser is serving an old cache; clear it and reload. If
it reads `index-CR5oJEHh.js` and the numbers are still `$0`, that is a real bug —
screenshot the card and the pool's URL and tell me.

### Task 1b — Click all four commissioner sections once (2 minutes)

The other thing I could not verify, and the only outstanding check on #324. You
are already on the right page from task 1, so this is four clicks.

Stay signed in, on the same pool's **Commissioner** view.

1. Click **Overview** in the section nav.
   **What you should see:** the bento cards from task 1, the Record Payouts card
   (only if the pool is finalized — on an OPEN pool it is correctly absent), and
   the "Commissioner Control Room" header strip.

2. Click **Members & Payments**.
   **What you should see:** the "Member Roster & Payments" panel, full width —
   noticeably wider than it used to be, because it no longer shares a row with
   the scoring console. A member count chip on the right, and a **Remind** button
   on each row.

3. Click **Scoring**.
   **What you should see:** the "Week N Scoring Feed" card with Total Matchups
   and Completed Games, and the red **Score & Recap Week N** button.
   ⚠️ **Do not click Score & Recap** — that scores the week for real.

4. Click **Settings**.
   **What you should see:** the "Pool Rules & Settings Editor", and below it the
   gold-bordered **Exceptions — Commissioner Tools** collapsible. Scroll to the
   bottom: there is **exactly one** green "Save Pool Settings" button. There used
   to be five scattered through the page; four were deliberately removed.

5. Optional but useful — make a harmless edit and save it. Change any field in
   Settings (for example the pool description), click **Save Pool Settings**, and
   then click **Overview** while it is saving.
   **What you should see:** the green success banner appears and **stays visible
   after the section changes**. It is deliberately rendered outside the sections
   so a save started on Settings still reports its result.

**If any section renders blank or a control is missing:** that is exactly the
failure this change risks — a JSX block that landed outside every section
compiles and renders nothing. Tell me which section and what is missing, and
revert is one command:

```powershell
git -C "D:\march-melee-pools" revert --no-edit 245f3d4
```

Then push and rebuild. Nothing else depends on #324, so reverting it is safe and
does not touch the ledger fix from #322.

**If everything renders:** nothing further is needed — that closes the last open
verification from this session.

### Task 2 — Six orphaned worktree directories, and one held work that exists nowhere else

I pruned twelve worktrees; git deregistered all twelve. **Six** directories under
`.claude/worktrees\` are now on disk but unknown to git — the two whose deletion
Windows blocked while I was pruning, plus **four that predate this session and
that I did not create.** None of the six has `.git` metadata any more, so git
cannot tell you whether they hold uncommitted work, and neither can I.

> 🛟 **I nearly told you to destroy something.** An earlier draft of this task
> listed `harness-trust-boundary-aa538c` as "certainly safe to delete". It is not:
> it contains **`PLAN-TRUST-BOUNDARY.md`**, a 1,801-byte audit of this harness's
> own permission model, and that file is in **no commit on any branch** —
> `git log --all -- "*PLAN-TRUST-BOUNDARY*"` returns nothing, and it is absent
> from the working tree. Following my own instruction would have irreversibly
> deleted the only copy.
>
> **It is rescued.** This PR adds it at the repo root as `PLAN-TRUST-BOUNDARY.md`,
> byte for byte, so it is in git history from now on. I have not acted on it and
> it has had no review — read it and decide. Its headline claim is that your
> global `~/.claude/settings.json` sets `defaultMode: bypassPermissions` with no
> deny rules, so the harness never prompts for anything, and that every safety
> rule in CLAUDE.md and the skills is prose the harness itself never enforces. It
> references two `scratchpad/` files I could not find; those are probably gone.
>
> My error is worth naming because it is cheap to repeat: I measured the folder
> with `find ... -maxdepth 2 | wc -l`, got "2 entries", and read that as empty.
> The two entries were the directory and the file. **I counted instead of
> looking** — the same class as the retraction in HANDOFF item 10.

1. Open **PowerShell**.
2. Go to the repo:

```powershell
Set-Location "D:\march-melee-pools"
```

3. See which directories git still tracks:

```powershell
git worktree list
```

**What you should see:** about twelve entries, including `D:\march-melee-pools`
itself, `bento-truth`, `commish-tabs`, and several older ones. **None** of the six
names in step 4 should appear.

4. See what is actually on disk:

```powershell
Get-ChildItem -Name "D:\march-melee-pools\.claude\worktrees"
```

**What you should see:** the tracked names from step 3, plus these six orphans —
`callable-sweep-batch6`, `harness-trust-boundary-aa538c`,
`interesting-volhard-481f5f`, `nfl-preseason-pilot-overnight-08b7b3`,
`scheduled-job-export-guard-3e783c`, `test-suite-phase1`. **Leave
`harness-trust-boundary-aa538c` alone** until you have read the rescued
`PLAN-TRUST-BOUNDARY.md` in the repo root and are satisfied the copy is good.

5. **Three are safe to delete.** I verified these by LISTING their files, not by
   counting entries: each holds zero files outside `node_modules`. Delete them one
   at a time:

```powershell
Remove-Item -Recurse -Force "D:\march-melee-pools\.claude\worktrees\interesting-volhard-481f5f"
```

```powershell
Remove-Item -Recurse -Force "D:\march-melee-pools\.claude\worktrees\nfl-preseason-pilot-overnight-08b7b3"
```

```powershell
Remove-Item -Recurse -Force "D:\march-melee-pools\.claude\worktrees\scheduled-job-export-guard-3e783c"
```

**What you should see:** each command prints nothing and returns to the prompt.

**If one fails with "being used by another process":** close any editor or
terminal that has that folder open, then run that one command again. It is fine
to leave it — an orphan folder is inert, just wasted disk.

6. **Two hold real file trees and I will not tell you to delete them blind.**
   `test-suite-phase1` (187 entries) and `callable-sweep-batch6` (224 entries)
   were working copies whose git metadata is gone, so if either contains an edit
   that was never committed, that edit exists only as a file and git cannot
   recover it. Look before deciding:

```powershell
Get-ChildItem -Recurse -File -Path "D:\march-melee-pools\.claude\worktrees\test-suite-phase1" | Where-Object { $_.FullName -notlike "*\node_modules\*" } | Sort-Object LastWriteTime -Descending | Select-Object -First 15 FullName, LastWriteTime
```

⚠️ **Note the `Where-Object`, and do not shorten it to `-Exclude node_modules`.**
`-Exclude` filters the items RETURNED, not the directories recursed INTO, so with
`-File` it prunes nothing — on `callable-sweep-batch6` that means scanning 54,254
dependency files and getting dependency artifacts as the "most recent" answer,
which is precisely the wrong input to a delete decision. The form above still
walks `node_modules` and so can take a minute; let it finish.

**What you should see:** the fifteen most recently touched files that are actually
part of the project. If they are all ordinary checked-in source with old
timestamps, there is nothing to save — delete the folder with the same
`Remove-Item` form as step 5. If you see anything you do not recognise, leave it
alone and tell me; these cost disk and nothing else.

Repeat the same command for `callable-sweep-batch6` before deciding on it. **Do
this before deleting either one** — that is the entire lesson of the box above.

7. **Do NOT delete any other folder in that directory.** The rest are live
   worktrees holding unmerged work — `advisory-board-recommendations-172341`,
   `attack-surface-inventory-871b84`, `nfl-monetization-strategy-6b52e4`,
   `handoff-docs-cleanup-cc9728` (which has **35 uncommitted files**),
   `ultracode-metrics-evals-d5b9b1`, `vet-youtube-video-docs-5e566c`, the two
   detached-HEAD ones, and `bento-truth` / `commish-tabs` from tonight. I left
   every one of those alone.

**Also worth knowing:** `gh pr merge --delete-branch` did **not** delete the
remote branches. `origin/claude/bento-truth` is still there, and so are the
branches for #319, #320 and #321. That is a repo setting, not a failure, but it
means the remote branch list will keep growing.

### Task 3 — Decide whether the remaining queue order still stands

The queue you left was A, B, C, D, E. **A is done.** My honest read, for you to
overrule:

- **PR C (surface `system/heartbeats` in the SuperAdmin UI)** has the highest
  operational value before the HOF game. Right now every scheduled job's liveness
  and verdict is invisible unless you open the Firebase console, and you will
  want that visible on game night.
- **PR D (importer safety)** is the only correctness item with a known
  production consequence — regular-season week 1 has **zero documents** in prod
  today. It does not affect the HOF pilot, but it bites in September.
- **PR B (tabbed commissioner split)** is pure layout. It is the largest and
  riskiest mechanical change of the three and buys the least before a deadline.

Nothing needs doing here unless you disagree with the order. If you want me to
reorder, say so; otherwise I will keep going in your order.

### Task 4 — A8 pricing is the only calendar-bound item and it is due 2026-08-06

Unchanged from previous mornings, restated because the date is now six days out.
Publishing the 2026 price and the free-period end date is yours; nothing in the
codebase blocks it.

### Task 5 — Backups are still the biggest open risk

Also unchanged and also yours: PITR, backup schedules, the GCS bucket and IAM are
all console work I cannot do. `PLAN-BACKUPS-PHASE3.md` has the detail. The
database is `nam5`, which supports PITR. Once those exist, the scheduled Auth
export is deferred **code** (step 6), not a console click — that part is mine.

---

## 5. What I did NOT get to, and why

**PR B — tabbed commissioner split. ✅ DONE after this document was first
written.** See §2b. It is merged as `245f3d4` (#324) and rebuilt.

**PR C — `system/heartbeats` reader. Not started.** See task 3; I think this
should go next, and it is now the top of the queue.

**PR D — `PLAN-IMPORTER-SAFETY.md` and the importer fix. Not started.** No
re-import was run, not even a dry run.

**PR E — `PLAN-BANTER-PANEL.md`. Not started.** PR #322 did change that card's
footer, which used to claim an active AI moderation capability, to say the panel
is a draft and is not saved. That is an honesty fix, not the feature; the panel
is still local React state that vanishes on refresh, exactly as recorded in
HANDOFF item 8.

**The `csp-verify` throwaway issue in Sentry. Not resolved.** I never opened the
Sentry dashboard this session — nothing required it.

### New finding, recorded not fixed (HANDOFF item 12)

**The Submission Health card is blind the same way the ledger was.**
`submissionStats` and `unsubmittedPlayers` on `NFLManagerBentoDashboard` are
still `entries`-derived, so a member who joined and has not submitted an entry is
counted in NEITHER the total nor the pending list. A pool with members but no
entries reports "0 of 0 participants have locked in" at **100%**, which reads as
perfect submission health on a pool where nobody has picked anything. Same root
cause as the ledger and the same fix shape — point it at `buildPoolRoster`, which
already returns `hasEntry`. I left it out of #322 deliberately to keep that change
to the money surfaces you scoped; it is a display defect, not a data-integrity
one, and the nudge list it feeds can only under-report, never over-report.

### Still open, not in tonight's queue

Unchanged: D11 (Props payment model, your product question), D22 (`isSuperAdmin`
rules bypass), D26 (Entries counter), 11 pools missing billing, the
`firebase-functions ^7` upgrade prompt, the `protobufjs` 7.6.4 moderate in
functions prod deps, the >24h stale-finalize arming prerequisite
(PLAN-REALTIME-SCORING §7), and the PR-B2 dry-run watch — which still only counts
if it SAW an event, since `queuedEvents: 0` proves nothing but that the scheduler
wrapper runs.
