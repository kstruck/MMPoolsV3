# MORNING <!-- hof-date:ignore --> 2026-08-07 — merge two PRs, deploy once, then repair the week-1 data

Written overnight. Everything below is measured, not remembered; each claim names
the command that produced it.

**Nothing was deployed, no production data was mutated, nothing was merged.** The
only production access taken was READ-ONLY, using the service-account key you
created (revoke it in Task 5).

---

## 0. TL;DR — four tasks, in this order

| # | Task | Risk |
|---|---|---|
| 1 | Merge #392 and #384, then ONE functions deploy | deploy |
| 2 | Re-import preseason weeks 1 and 2 (the data repair) | production data |
| 3 | Re-score weeks 1 and 2 on the affected pools | production data |
| 4 | Verify | read-only |
| 5 | Revoke the service-account key | security hygiene |

**Do not reorder 1 and 2.** Running the import against the currently-deployed
importer deletes all 49 preseason games and wipes the HOF game's locked spread.

---

## 1. State as of this morning

| Fact | Value | How it was measured |
|---|---|---|
| `origin/main` | `e59f92e` | `git rev-parse origin/main` |
| Functions deployed from | <!-- deploy-state:ignore --> `main` @ `1105392` | unchanged; HANDOFF carries the authoritative claim |
| Rules | ≡ `0a705c0` | unchanged |
| **Live frontend bundle** | **`index-W6uLtMV7.js`** | fetched from prod, see below |
| Auto-scoring | **OFF, and stays off** | `system/config.nflAutoScore` unset; `nflFinalize`/`nflSpreadLock` `{enabled:true, dryRun:true}`; `nflDeepSweep` unset |
| Score ingestion | **automatic** | `syncNFLScoresJob`, every 5 min, no kill switch |
| Grading | **manual** | there is no scheduled scorer |

⚠️ **HANDOFF was stale about the bundle and is corrected in this PR.** Its STOP
POINT box still said `index-C9jX7I-m.js`, which was true on 2026-08-06 before
#385–#388 merged. Production now serves:

```
status 200   https://www.marchmeleepools.com/
entry assets: index-W6uLtMV7.js, index-Dl2F2o_4.css
```

### Neither PR owes a Coolify rebuild — re-measured, because the first answer was wrong

`#392` touches no client file at all:

```
git diff --stat origin/main...origin/claude/nfl-import-scope -- src/ shared/ index.html public/
```

is **empty**.

`#384` DOES touch `src/utils/nflWeekLabel.ts` and adds `shared/nflWeekLabel.ts`, so
its "no rebuild owed" claim was re-verified by building both trees on one machine
with one `.env`:

| build | entry assets |
|---|---|
| `origin/main` | `index-3vC8k8ii.js`, `index-BqYjhA29.js`, `index-Dl2F2o_4.css` |
| `origin/main` **merged with** #384 | `index-3vC8k8ii.js`, `index-BqYjhA29.js`, `index-Dl2F2o_4.css` |

Identical (`md5` of the sorted asset list matches: `d54d8de7…`). `src/utils/nflWeekLabel.ts`
becomes a re-export of `shared/nflWeekLabel.ts` with byte-identical logic, so the
emitted bundle does not move.

> 🪤 **The first attempt at this said the assets DID differ, and that was wrong.**
> It built #384's branch tip, which is **5 commits behind `origin/main`** — so the
> difference came from the commits main had gained, not from #384. This is the
> two-dot-versus-three-dot trap in build form. Compare `main` against
> **`main` merged with the branch**, never against the branch tip.

---

## TASK 1 — Merge #392 and #384, then deploy functions ONCE

Both are `functions/`-coupled, both are `MERGEABLE`, both are 7/7 green.

| PR | Head | What it is |
|---|---|---|
| [#392](https://github.com/kstruck/MMPoolsV3/pull/392) | `a31a130` | importer hardening — files games by ESPN's week, scopes the cleanup, preserves locked spreads |
| [#384](https://github.com/kstruck/MMPoolsV3/pull/384) | `d58e9b3` | `TEAM_ALREADY_USED` re-submit guard + scoring speaks in week LABELS |

### Step 1.1 — Read the review record on #392 first

Open https://github.com/kstruck/MMPoolsV3/pull/392 and read the comments I left
overnight. The short version:

- **8 codex rounds.** r1 and r2 each found a P1 in `syncScoresWindow` (a spillover
  terminal transition, then a spillover stat correction — the second holed the
  reasoning I had written into the first one's commit message). r3 found a P2 (a
  withdrawn unlocked spread surviving `merge: true`). r6 found that two of my own
  earlier fixes had collided. r7 found one restatement paging twice. **r4, r5 and
  r8 clean.**
- **5/5 qodo findings resolved** across two review passes — four fixed with tests,
  one rejected in writing with this repo's own prior decision as the evidence.

Every guard added is mutation-checked; each commit names its mutant and what it
breaks.

⚠️ **The codex round budget is 8 of 10 used** (CLAUDE.md §2c). If a further round
finds something, I need your say-so before spending past 10.

If you disagree with the rejection of qodo #7 — it asked for a per-file emulator
guard that `setup.ts` + `vitest.emulator.config.ts` already provide centrally, a
design this repo adopted in response to qodo itself on #332 — stop and tell me.
Everything else is closed.

### Step 1.2 — Merge both

```bash
gh pr merge 392 --squash --delete-branch
```

```bash
gh pr merge 384 --squash --delete-branch
```

**What you should see:** `✓ Squashed and merged pull request #392` and the same for
#384.

**If #384 reports a conflict:** it is 5 commits behind main. I merged the two
locally overnight and it was clean, so a conflict would be new. Do not force
anything — tell me and I will rebase it.

### Step 1.3 — Get main locally

```bash
cd D:\march-melee-pools
```

```bash
git checkout main
```

```bash
git pull
```

**What you should see:** the two squash commits at the top of `git log --oneline -3`.

### Step 1.4 — Install functions dependencies

⚠️ **`ci`, not `install`.** `install` rewrites the lockfile and dirties the tree
that `firebase deploy` packages.

```bash
npm --prefix functions ci
```

**What you should see:** it finishes with an `added N packages` line and no error.
Warnings about `allow-scripts` are normal and can be ignored.

### Step 1.5 — Deploy functions

```bash
npx firebase deploy --only functions --project gridiron-gamble-uzuqo
```

**What you should see:** a list of functions, each `✔` updated or skipped, ending
in `✔ Deploy complete!`.

**If it asks you to authenticate:** run `npx firebase login` first, then repeat.

**If a function fails to deploy:** do NOT retry blindly. Copy the error and stop —
a partial functions deploy with the importer half-updated is exactly the state
Task 2 must not run against.

### Step 1.6 — Certify the deploy with a second run

```bash
npx firebase deploy --only functions --project gridiron-gamble-uzuqo
```

**What you should see: every function reported as `Skipped (No changes detected)`.**
That all-`Skipped` second run is the certification — it proves the first run
actually landed rather than reporting success on a no-op.

**If anything is NOT skipped on this second run,** the first deploy did not fully
land. Stop and tell me.

### Step 1.7 — No rules deploy, no Coolify rebuild

Do **not** run a rules deploy — `firestore.rules` is untouched (still ≡ `0a705c0`).
Do **not** trigger a Coolify rebuild — measured above, the bundle does not move.

---

## TASK 2 — The data repair: re-import preseason weeks 1 and 2

⛔ **ONLY after Task 1's step 1.6 shows an all-`Skipped` run.** Against the old
importer this deletes all 49 preseason games and wipes the HOF game's locked
spread.

### What is wrong right now

Production `nfl_games`, season 2026 seasonType 1, currently holds:

| week | stored | correct |
|---|---|---|
| 1 | **7** | 1 |
| 2 | **10** | 16 |
| 3 | 16 | 16 |
| 4 | 16 | 16 |

Six games kick off Aug 13/14 and belong to week 2 but are filed under week 1:
`espn_401873272` (DET@CIN), `espn_401873275` (GB@PIT), `espn_401873273` (IND@NE),
`espn_401873274` (LAC@HOU), `espn_401873640` (ARI@LV), `espn_401874392` (TEN@SF).

### Step 2.1 — Run the import

1. Open https://www.marchmeleepools.com/super-admin
2. Go to the **NFL Schedule** section.
3. Set **Season** to `2026`.
4. Set **Season Type** to **Preseason** (`1`).
5. Select weeks **1 and 2** — both, in one run.
6. Click the import button.

**What you should see:** a success result reporting an imported count. With the
hardened importer, week 1's over-returning query still yields 7 events, but each is
now filed by ESPN's own `week.number`.

**If it errors:** copy the message and stop. Do not re-run repeatedly.

### Step 2.2 — Verify the counts

Same PowerShell window as before (or re-do Task 5's step 1 to set the key):

```bash
cd D:\march-melee-pools\.claude\worktrees\mmp-overnight-session-8a3d23
```

```bash
node .claude\skills\mmp-diagnostics-and-tooling\scripts\confidence-exposure-detail.mjs
```

**What you should see:** the `week 1 as stored today:` block listing **only**
`espn_401873271 CAR@ARI`, and no `-> MOVES TO WEEK 2` lines.

**Expected final state:** week1 = 1, week2 = 16, week3 = 16, week4 = 16.

### Step 2.3 — Verify the locked spread survived

In the same output, or in the Firestore console at `nfl_games/espn_401873271`,
confirm **`spread.locked === true`** and the value is still **−1.5**.

**If the spread is unlocked or gone,** stop immediately and tell me — that is the
failure mode #392 exists to prevent, and it would block every ATS pick behind
`SPREADS_NOT_LOCKED`.

---

## TASK 3 — Re-score weeks 1 and 2

The import fixes the games. **It does not re-score anything**, and nothing
re-scores on its own — grading is manual (see §1).

### Step 3.1 — For each affected pool

The seven NFL preseason pools are listed at the end of this document. For each one
that has entries:

1. Open the pool.
2. Click the **COMMISSIONER** button — the gear icon in the pool header, to the
   right of **INVITE LINK**. (There is no "Manager" tab. The tab strip is Pool Home
   / My Entry / Standings & Leaderboard / Weekly Recaps / Rules & Rulesets /
   Payments.)
3. Run **Score & Recap** for **week 1**.
4. Run **Score & Recap** for **week 2**.

Scoring is idempotent — the Survivor `strikeWeeks` ledger has set semantics and
`computeSurvivorWeekUpdate` recomputes per `(entry, week)` — so re-clicking is safe.

### Step 3.2 — The confirmation to look for

For week 1 you should now see **"Week 1 scored successfully."** instead of
**"scored provisionally"**.

That is the strongest single signal that the repair landed, and it is better than
the counts: week 1 afterwards holds one FINAL game, so `isWeekComplete` is
satisfied. It depends on slate membership rather than on a number we could also
have read wrong.

**If it still says "provisionally",** the import did not move the six games. Stop
and tell me.

---

## TASK 4 — What I already verified so you don't have to

**Member picks are NOT at risk from this repair. Exposure measured at ZERO.**

I flagged a real hazard yesterday and then measured it against production; it does
not bite here. Both mechanisms are real, and both are documented in
`PLAN-NFL-IMPORT-SCOPE.md` §6b for the next time:

- confidence values are validated **only at submit** and scoring just sums the
  stored numbers, so changing the number of games in a week can leave a value out
  of range or colliding;
- Survivor/Margin picks are keyed by **week** holding a **team**, so a pick does
  not follow its game across weeks — a stranded Survivor pick grades as survived
  with no strike, and a stranded Margin pick is skipped entirely with no `-14`.

Measured before the repair:

```
pools scanned: 134   NFL preseason pools: 7
(a) NFL_PICKEM pools with confidenceMode === true ......... 3
(b) confidence entries holding week 1 or 2 picks ........... 4
    of those, stored value out of range after the move .... 0
    of those, colliding value inside week 2 after ......... 0
(c) Survivor/Margin pools with a stranded week 1 pick ...... 0
```

Why the zeros are real rather than a filter stepping over gaps:

- all four confidence entries hold exactly one week-1 pick, on `espn_401873271`, at
  confidence **16**, with **0 MISSING** values — and 16 is the only legal value in
  a one-game week, so every sheet is legal before and after;
- week 2 only **widens** (`[7..16]` → `[1..16]`), so nothing arriving there can go
  out of range;
- the surviving week-1 game is **CAR@ARI**, and the Margin entry that looked exposed
  had picked **ARI**, which still plays in week 1. Not stranded;
- nothing has been scored yet on any affected pool (`scoredWeeks`, `publishedWeeks`,
  `scoredThroughWeek` all unset; `weeklyScores` empty; `seasonTotal` 0), so there is
  no banked per-week value to go stale.

> ⚠️ **My first census reported one exposed Margin entry and it was a FALSE
> POSITIVE** — it asked "is this team in a moving game", which flags ARI, when the
> right question is "in a moving game **and** left with no game in the week
> afterwards". Fixed, and the trap is written into the script and the plan, because
> the next re-file will meet it again.

**One thing the repair silently FIXES.** Week 1 currently contains both
`espn_401873271` CAR@**ARI** and `espn_401873640` **ARI**@LV, and `scoreMarginWeek`
resolves a pick by first-match array order. So an ARI Margin pick is, today, graded
against whichever game the query returned first. One entry is in that state. After
the repair the ambiguity is gone — if that grade changes, it is the fix, not a
regression.

---

## TASK 5 — Revoke the service-account key

You created `C:\keys\gridiron-admin.json` for the census. It is a long-lived
credential with full Firestore access.

**Keep it until Task 2 step 2.2 is done** (the verification script needs it), then:

1. Open https://console.cloud.google.com/iam-admin/serviceaccounts?project=gridiron-gamble-uzuqo
2. Click the service account whose name contains **`firebase-adminsdk`**.
3. Open the **KEYS** tab.
4. Find the key created <!-- hof-date:ignore --> 2026-08-07 and click the trash icon, then confirm.
5. Delete the local file:

```bash
Remove-Item "C:\keys\gridiron-admin.json"
```

**What you should see:** the key disappears from the KEYS list and `Test-Path
"C:\keys\gridiron-admin.json"` returns `False`.

To re-run the census later, generate a fresh key and repeat.

---

## Reference — the seven NFL preseason pools

Measured read-only. Pool ids only; no member names or emails.

| Pool id | Type | Confidence | Entries touching weeks 1–2 |
|---|---|---|---|
| `0ybpLzY7fJ3NJbDj0j1l` | NFL_PICKEM (ATS) | yes | 3 |
| `2mv4pKI734hHeQHzQVTP` | NFL_MARGIN | — | 2 |
| `AaacUZimEemd7WM2YgQt` | NFL_PICKEM | yes | 0 |
| `BJunCb6pMyX6kRwptUSm` | NFL_PICKEM | no | 0 |
| `CPW8fbmyausHsgig8i2t` | NFL_SURVIVOR | — | 1 |
| `qXm0h0qFyjDcaXjvYceH` | NFL_PICKEM | yes | 1 |
| `xsOlUi1FVOyqHuqgM1Bj` | NFL_PICKEM | no | 0 |

Only the four with entries need Task 3.

---

## What I did NOT do, and why

- **Did not merge anything.** You asked to be told first (#392), and #384 was
  already deliberately held.
- **Did not deploy.** Gated on you.
- **Did not run the import or touch any production document.** All production
  access was read-only.
- **Did not touch #380 / #352 / #304 / #302 / #300.**
- **Did not fix the three remaining npm audit moderates.** Every available fix is a
  major DOWNGRADE (`firebase-tools` → 14.23.0, `firebase-functions-test` → 0.3.3),
  which is a worse trade than the advisory, and all three sit below the
  `--audit-level=high` gate. Accepted, not forgotten.

## Open items

- `PLAN-IMPORTER-SAFETY.md` defect 1 records that **regular-season (seasonType 2)
  week 1 held ZERO documents** when measured on 2026-07-30. That restoration is
  still owed and lands mid-season with picks and scores already banked — where none
  of the three reasons for today's zero exposure will hold. Re-run both census
  scripts before attempting it.
- `PLAN-IMPORTER-SAFETY.md` defects 2, 3 and 4 remain open: a fetch failure after
  the delete still leaves a hole (now one week deep, not a whole season), the result
  still always claims success, and the importer still has no kill-switch and no dry
  run.
