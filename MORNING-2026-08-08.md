# MORNING 2026-08-08 — the automated-scoring go-live, and what else landed overnight

Written overnight. Everything below is measured, not remembered; each claim names
the command that produced it.

**Nothing was deployed, no production data was mutated, and no config flag was
flipped.** Merges are the only production-adjacent thing done overnight, per your
standing rules. Every deploy, config flip and Coolify rebuild below is yours.

🧱 **This document GREW through the night.** Each PR opened overnight appended its
own section as it landed, in the order it landed. If a section you expected is
missing, that PR did not reach the gate — the "What did NOT get done" section at
the bottom names it and says why.

---

## 0. TL;DR — the order to do things in

| # | Task | Risk |
|---|---|---|
| 1 | Merge the hygiene PR (nanoid + docs). No deploy. | merge only |
| 9 | Revoke the service-account key | security hygiene |

Task numbers are **not** contiguous: they were assigned as the overnight PRs
landed, and a number with no section is a task that did not reach the gate. The
"What did NOT get done" section at the bottom names those. **Nothing in this list
is time-critical** — it can all wait for coffee.

---

## 1. State as of this morning

Read this before touching anything; it is the ground truth the rest of the
document assumes.

- **`origin/main` is `62ff437`**, and **functions are deployed from `62ff437`** —
  #392 (importer hardening) and #384 (`TEAM_ALREADY_USED` resubmit guard + week
  labels) both shipped in one full-fleet deploy, certified by a second run that
  reported every function `Skipped (No changes detected)`.
- **Rules are ≡ `0a705c0`.** `firestore.rules` is byte-identical since, so no
  rules deploy is owed.
- **The live frontend bundle is `index-W6uLtMV7.js`**, read off production at
  00:23 MT on 2026-08-08 (`status 200`, entry assets `index-W6uLtMV7.js` /
  `index-Dl2F2o_4.css`). **Nothing is owed on Coolify.**
- **The week-1 data repair is DONE and verified.** `nfl_games` 2026 /
  seasonType 1 is week1=1 (`espn_401873271`, CAR@ARI, `FINAL`), week2=16,
  week3=16, week4=16; `espn_401873271.spread` is `locked=true value=-1.5`.
- **HOF Weekend is scored** on the four pools that have entries — you did that
  manually through the commissioner Score & Recap button.
- 🛑 **Automated scoring is still OFF.** `system/config.nflAutoScore` is unset;
  `nflFinalize` and `nflSpreadLock` are `{enabled:true, dryRun:true}`;
  `nflDeepSweep` unset. `syncNFLScoresJob` (every 5 minutes, no kill switch) IS
  live, so score **ingestion** is automatic and **grading** is not.
- 🔐 **`C:\keys\gridiron-admin.json` is still ACTIVE.** Nothing in this document
  needs it, so it can be revoked first thing — see the last section.

---

## TASK 1 — Merge the hygiene PR (nanoid patch + doc state)

**One line:** merge the PR titled *"chore(deps,docs): patch nanoid in both
trees; record the 62ff437 deploy and the week-1 repair"*. It is lockfile-and-docs
only. **No deploy of any kind is owed by it.**

**Why it matters this morning:** the CI Security Scan on `main` has been RED
since <!-- hof-date:ignore --> 2026-08-07 on nanoid GHSA-2v37-7h3g-55p8 (HIGH). Merging this turns it
green and stops the emails.

**Where to be:** any browser, or a PowerShell window in `D:\march-melee-pools`.

### Steps

1. List the open PRs so you can see its number:

   ```powershell
   gh pr list
   ```

2. Confirm CI is green on it (substitute its number for `<N>`):

   ```powershell
   gh pr checks <N>
   ```

   **What you should see:** every check `pass`. In particular
   `Security Scan` must be `pass` — that is the whole point of the PR.

3. Merge it:

   ```powershell
   gh pr merge <N> --squash --delete-branch
   ```

   **What you should see:** `✓ Squashed and merged pull request #<N>`.

4. **Do nothing else.** No `firebase deploy`, no Coolify rebuild. The diff is
   two `package-lock.json` files, three markdown files, and one previously
   untracked script — none of it reaches production.

**If it does not work:** if `gh pr checks` shows `Security Scan` failing, do NOT
merge; a *new* advisory landed after the PR was built. Tell me and I will rebase
the bump. If the merge is blocked as `BEHIND`, run
`gh pr update-branch <N>` and re-check.

### What is in it, so you do not have to read the diff

- **nanoid 3.3.16 → 3.3.18 in both lockfiles.** GHSA-2v37-7h3g-55p8, HIGH,
  dev-only in both trees (root: `postcss` → `nanoid`; functions: `vitest` →
  `vite` → `postcss` → `nanoid`). `npm ls nanoid --omit=dev` is **empty** in both
  trees, so nothing shipped to Cloud Functions was ever exposed. No
  `package.json` changed — three lines per lockfile, the same shape as #390.
  After it, `npm audit --audit-level=high` exits **0** in both trees.
  The remaining **moderates are accepted, unchanged from #390**: root
  `@opentelemetry/core` (via `firebase-tools`) and functions `ts-deepmerge` (via
  `firebase-functions-test`) each need a breaking major to clear.
- **`restamp-preseason-weeks.mjs` is now tracked in git.** It is the script that
  performed the week-1 repair, and it was living untracked in the main checkout —
  the only record of how production data got changed. Its header now records the
  execution and the verified after-counts. Re-running it REFUSES, by design: it
  demands all six documents be in the pre-repair state and aborts otherwise.
- **HANDOFF.md and PICKUP-PRESEASON-PILOT.md** move the tagged live deploy claim
  from `1105392` to `62ff437` and demote the old boxes to historical.
- **MORNING-2026-08-07.md** gains four corrections at the top: the missing
  `FUNCTIONS_DISCOVERY_TIMEOUT` step, a multi-week import UI that does not exist,
  the repair having been done by the restamp script rather than the importer, and
  a re-score that was over-scoped to a week nobody has played.

---

## TASK 9 — Revoke the service-account key

🔐 **Nothing in this document needs `C:\keys\gridiron-admin.json`**, so this can
be done first thing rather than last. It is a long-lived credential with full
Firestore access, created <!-- hof-date:ignore --> 2026-08-07 for the read-only census.

### Steps

1. Open
   https://console.cloud.google.com/iam-admin/serviceaccounts?project=gridiron-gamble-uzuqo
2. Click the service account whose name contains **`firebase-adminsdk`**.
3. Open the **KEYS** tab.
4. Find the key created <!-- hof-date:ignore --> 2026-08-07 and click the trash
   icon, then confirm.
5. Delete the local file:

   ```powershell
   Remove-Item "C:\keys\gridiron-admin.json"
   ```

6. Confirm it is gone:

   ```powershell
   Test-Path "C:\keys\gridiron-admin.json"
   ```

**What you should see:** the key disappears from the KEYS list, and step 6 prints
`False`.

**If it does not work:** if the KEYS tab shows several keys and you cannot tell
them apart, sort by **Creation date** and revoke the <!-- hof-date:ignore --> 2026-08-07 one only — do not
touch older keys, which may be in use by other tooling. If `Remove-Item` reports
the file is in use, close any PowerShell window that has
`$env:GOOGLE_APPLICATION_CREDENTIALS` set and try again.

To run a census later, generate a fresh key and repeat.

---

## What did NOT get done overnight, and why

*(Filled in at the end of the run.)*
