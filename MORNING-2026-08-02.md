# MORNING — 2026-08-02

**HOF game: 2026-08-06 (Thu, 8:00pm ET = `2026-08-07T00:00Z`), CAR at ARI. Four days.**

## 0. TL;DR — you have NOTHING you must do

| | |
|---|---|
| **All three deploy queues** | ✅ **EMPTY.** Functions `4713eba`, rules ≡ `0a705c0`, bundle `index-Bv2FV3GO.js` |
| **Your Coolify rebuild** | ✅ **Verified.** `index-Db6JwMWs.js` → `index-Bv2FV3GO.js`, read off the prod HTML |
| **#341 — repair-job narrowing** | ✅ Merged + deployed |
| **#340 — setPaidStatus plan** | 🟡 Open. Prerequisite now satisfied; needs implementation |
| **#338 — reminder targets** | 🟡 Open. Needs the §4a canonical filter + a rebase |
| **6 dependabot PRs** | ❌ Rejected with evidence. Leave them |

**Nothing is broken and nothing is waiting on you.** §1 is only if you *want* to
verify last night's work; §2 is the copy-paste prompt for the next session.

---

## 1. Optional — verify last night's work (about 3 minutes)

Skip this entirely if you trust the write-ups. Nothing below changes anything.

Every command is PowerShell 5.1. One command per block, no `&&`.

### Step 1 — confirm the deployed code matches `main`

```powershell
Set-Location D:\march-melee-pools
```

```powershell
git fetch origin
```

```powershell
git log --oneline -3 origin/main
```

**You should see** `2252683` (the deploy-state docs) at or near the top, with
`4713eba` (the repair-job narrowing) below it.
**If you see something else:** someone pushed after I stopped — read
`HANDOFF.md`'s STOP POINT box before acting on anything in this file.

### Step 2 — confirm the live frontend is the one you rebuilt

```powershell
curl.exe -sSL https://www.marchmeleepools.com/ | Select-String -Pattern 'index-[A-Za-z0-9_-]+\.js'
```

**You should see** `index-Bv2FV3GO.js`.
**If you see `index-Db6JwMWs.js`:** the rebuild did not take after all — tell the
next session and it will re-check the Coolify log.
**If you see a third, different hash:** something rebuilt after you did. Not
necessarily wrong, but say so before the next session deploys anything.

### Step 3 — see the counter you rebuilt for

This is the only user-visible thing #341 added.

1. Open <https://www.marchmeleepools.com/> and sign in as yourself.
2. Go to **SuperAdmin → Operations**.
3. Find the **roster backfill** control (`backfillMemberRecords`).
4. Run it **in dry-run** — it defaults to dry-run, so just run it.
5. **What you should see:** the Run Log now includes a **`squaresSkipped`** count
   alongside `guestSkipped`.
6. **What it means:** the number of square-owning uids the job REFUSED to promote
   to roster membership. **On a pool with no squares it will be 0, which is
   correct and expected** — do not read 0 as "the change did not work".
7. **If `squaresSkipped` is missing from the Run Log:** the frontend rebuild did
   not include it. Tell the next session.

⛔ **Do not flip it out of dry-run.** Nothing needs that, and it is a Rule 1
prod-data gate.

---

## 2. Copy-paste prompt for the next session

Paste everything in the box below into a fresh session.

```
Continue March Melee Pools. Repo D:\march-melee-pools — work in fresh worktree
branches off origin/main.

⏰ HOF GAME 2026-08-06 (Thu 8:00pm ET, CAR at ARI). Four days. First 16-game
slate 2026-08-13. Prioritise by "does this break the pilot".

READ FIRST: CLAUDE.md, HANDOFF.md STOP POINT box, PICKUP-PRESEASON-PILOT.md §0,
MORNING-2026-08-02.md.

I am away. Keep working without per-chunk approval. Leave a MORNING doc
(format: MORNING-2026-08-02.md, tasks in PowerShell 5.1 — no &&, one command
per block).

═══════════════════════════════════════════════════════
STATE — VERIFY, DO NOT TRUST
═══════════════════════════════════════════════════════
  git log --oneline -3 origin/main
  gh pr list
  curl -sSL https://www.marchmeleepools.com/ | grep -o 'index-[A-Za-z0-9_-]*\.js'

- origin/main was 2252683 when this was written. Functions deployed from
  4713eba (evidence: second full-fleet run, 173 all "Skipped (No changes
  detected)"). Rules ≡ 0a705c0. Live bundle index-Bv2FV3GO.js. ALL THREE
  QUEUES EMPTY unless a merge since changed functions/, shared/, rules or src/.
- Dependabot #299–#304: do NOT merge. All six rejected with evidence 2026-08-01
  (five have failing CI; #304 has green CI but does not merge cleanly).
- system/config.nflAutoScore + statsRecompute UNSET = disabled. LEAVE THEM.

QODO IS ON. Check all three surfaces on every PR:
  gh api --paginate repos/kstruck/MMPoolsV3/issues/<n>/comments
  gh api --paginate repos/kstruck/MMPoolsV3/pulls/<n>/comments
  gh api --paginate repos/kstruck/MMPoolsV3/pulls/<n>/reviews
Stopping rule is JOINT: qodo clean AND codex clean AND your own diff read.
It has earned it — on 2026-08-01 qodo found FOUR defects that ten codex rounds
and self-review missed, including one introduced in the same session.

⛔ APP CHECK: do NOT set VITE_RECAPTCHA_SITE_KEY, do not "fix" the "App Check is
NOT active" warning — it is the safe state. Root cause OPEN. Record in HANDOFF.

⛔ SQUARES SECURITY: SECURITY-CLAIM-SQUARES.md describes a KNOWN, ACCEPTED-
THROUGH-THE-PILOT hole (guestDeviceKey is a bearer token in a world-readable
doc). Standing decision, HANDOFF §5: fix before the regular season, NOT now.
Its real fix needs a prod backfill, which is mine to run. Do not start it.

═══════════════════════════════════════════════════════
AUTONOMY GRANT
═══════════════════════════════════════════════════════
 ✅ Merge your own PRs when all 7 CI checks pass AND codex clean AND qodo
    reported-and-resolved AND your own read agrees. A PR carrying an
    unresolved finding is MINE to merge — say so and stop.
 ✅ Deploy functions when a PR needs it: npm --prefix functions ci (ci, NOT
    install), npx firebase deploy, functions BEFORE rules. Run it from a
    directory that HAS the CLI — the main checkout has no root node_modules
    unless you run npm ci there; `git worktree list` to pick one. Re-run
    --only functions until EVERY function reports "Skipped (No changes
    detected)" — that run is the evidence. Confirm the PR is actually IN
    origin/main first, or an all-Skipped run certifies a deploy that never
    happened.
 ✅ Trigger Coolify rebuild for src/** changes. Use the Chrome extension
    (authenticated); the in-app browser hits a login page. NEVER enter
    credentials. Verify the bundle hash moved — but an UNCHANGED hash is
    inconclusive, not proof of failure (Vite can emit an identical bundle);
    read the Coolify log before calling it failed.
 ✅ Read prod, Sentry, Firebase console.

 ⛔ No prod-data writes. No arming scheduled jobs. No dependabot merges. No
    Coolify env-var changes.

═══════════════════════════════════════════════════════
QUEUE — one PR at a time, all gates, both reviewers
═══════════════════════════════════════════════════════

──── TASK 1 — finish the #340 → #338 chain (do first) ────
These two are the last pilot-relevant correctness work and they are coupled.
Read PLAN-SETPAIDSTATUS-MEMBERSHIP.md §0 and §4a FIRST — nine review rounds are
recorded there and the design shrank twice under review; do not re-derive it.

 a. #340's prerequisite is now SATISFIED (#341 merged+deployed). Flip §0's
    status, then IMPLEMENT the guard the plan specifies: setPaidStatus's claim
    branch must verify membership before writing. TWO evidence checks only —
    a CANONICAL Member Record (server-seeded `joinedAt`, per planMembershipWrite)
    or `participantIds` minus the "guest" sentinel. All reads inside the
    transaction, and the transactional pool snapshot must EXIST. There is no
    third check; round 5 removed the squares one as attacker-settable.
    Ship NOT_A_POOL_MEMBER: with a client mapping, and put the contract test in
    tests/error-domain-prefix-contract.test.ts covering that prefix ONLY —
    #338's branch adds MEMBER_NOT_ON_ROSTER cases to the same file after rebase
    (plan D3 records why a combined test cannot compile here).
 b. Then #338: add the §4a CANONICAL FILTER to resolveReminderTargets — it
    currently accepts every members document regardless of fields, so a record
    forged before (a) still receives reminders. Without this neither PR closes
    the exposure. Then rebase onto main and re-run npm test.
 c. Merge order is (a) then (b). #338 is at 12+ codex rounds already; it is
    past the cap, so re-review only what you change.

──── TASK 2 — system/heartbeats UI ────
NOTHING renders system/heartbeats (functions/src/lib/heartbeat.ts:46,84). Only
reader is the Firebase console, so the whole withHeartbeat investment is
invisible on game night. findStaleJobs already computes never-ran/stale
verdicts server-side, with tests — it needs a reader, not new logic. Read-only
card on the SuperAdmin Overview tab (the existing "Ops Health" card is NOT it —
that is monetization alerts). Respect the eight-tab invariant in CONTEXT.md.
Honest empty states — never fabricate a verdict. Extend
tests/admin-surface-invariants.test.ts.
📌 #333 added a `scorelessFinals` signal the card should surface: a FINAL game
the feed reported no scores for is the HOF-night condition that used to grade
as a real 0-0.

──── TASK 3 — only if time remains ────
Widen tests/docs-state-invariants.test.ts to catch PROSE-vs-tag drift. It pins
tagged SHAs and passed through THIRTEEN instances of heading-lags-content on
2026-08-01 — headers, banners, refresh notes and evidence paragraphs going
stale while the tag beside them was correct. Five consecutive review rounds
each found a different one in the same two files. Docs-only, zero pilot risk.

Then: PLAN-IMPORTER-SAFETY.md (docs only; ⛔ NO re-import, not even dry-run),
and worktree pruning per MORNING-2026-07-30 Task 2.

═══════════════════════════════════════════════════════
RULES
═══════════════════════════════════════════════════════
- Gates per PR: functions build, functions test (1229), emulator, tsc -b,
  npm test (498), lint (0 errors). Baselines at 4713eba. Fresh worktree:
  npm ci AND npm --prefix functions ci AND node functions/scripts/copy-shared.mjs.
  Don't run npm test concurrent with functions ci.
- codex exec review --base origin/main (three-dot scope check; -b never -B).
  Up to 10 rounds; past 10, stop, write the residual into the PR body, leave
  it for me.
- MUTATION-TEST EVERY GUARD. ⚠️ Repo files are CRLF — patterns fail SILENTLY
  and fake "mutation survived". ASSERT the anchor matched. Also verify the
  mutant landed in the RIGHT function: participant.ts has two `if (updated)`
  blocks and I hit the wrong one on 2026-08-01. A mis-targeted mutant is not a
  result — re-run it.
  Commit BEFORE mutation loops — git checkout in the loop eats uncommitted work.
- An ABSENCE (a scan deliberately removed) can only be guarded by a source
  invariant. Assert on code with comments stripped, or the explanation of why
  the code is gone will satisfy or break the test.
- Sweep before codex round 1, and make the sweep REPRODUCIBLE from its own
  stated commands — a sweep whose table cannot be re-derived is worse than none.
- Report faithfully: a missing error is not a pass.
- Known flake: opsAlertDispatcher.test.ts — re-run before investigating.
```

---

## 3. What happened last night, briefly

**#341 shipped the prerequisite.** The repair jobs (`fixParticipantIds`,
`backfillMemberRecords`) were promoting a guest-square claim into roster
membership — a stranger with a pool link could claim an unclaimed guest square,
and the next operator run turned that into `participantIds` membership and a
`joinedAt`-stamped Member Record. Square ownership now enriches a member but
never introduces one.

**Your option-2 call was the right one, and for a reason that only surfaced
afterwards:** option 1's last step is a prod backfill that is yours to run, and
a standing decision already deferred it. It could not have finished before the
pilot.

**The cost, stated plainly:** while `guestDeviceKey` is public the system cannot
tell a legitimate guest-square claimant from an attacker — both present the same
readable key — so neither becomes a roster member via these jobs. That includes
people who claimed legitimately before the deploy. `squaresSkipped` reports them
per run. Squares pools are not in the pilot, so the impact today is nil.

**Two of my own mistakes were caught:** I added a `participantIds` write to
`claimByCode` calling it "vetted" — it is not, `createClaimCode` is
`auth: "public"` — and gating the enrichment reordered a read so a nameless
participants doc wiped a real `userName`. The first was codex, the second qodo.
