# MORNING 2026-08-24 — overnight build of PLAN-WIZARD-BUYFLOW-FIXES

**Session: 2026-08-23 22:00 → 2026-08-24 05:30 ET, local (`D:\march-melee-pools`).**
**Ten PRs merged. One PR built and deliberately left open — the launch flip.
Two tickets NOT built (T8, T10). One verification NOT run (the T4 ledger audit).
One pre-existing problem found: the Playwright e2e suite is fully red, and was
already red before this session.**

Everything Kevin has to *do* is in the chat message that came with this
document. This file is the archival copy and the detail behind it.

---

## 1. What shipped

Every PR below is **merged to `main`**. Every one ran: root vitest, functions
vitest where `functions/` changed, the emulator rules suite where
`firestore.rules` changed, `tsc -b`, a full-repo lint-warning diff against a
stashed baseline, and `codex exec review --base origin/main` to a clean round
(CLAUDE.md §2c, qodo DORMANT ⇒ two conditions: clean codex round **and** my own
read of the diff agreeing).

| PR | ticket | what it fixes | codex rounds | deploy needs |
|---|---|---|---|---|
| [#522](https://github.com/kstruck/MMPoolsV3/pull/522) | **T2** | A coupon could remove the only paid path. `Activate now` rendered on `quote.total > 0`, so a 100%-off coupon made it vanish — while the server has supported $0 activation all along. | 3 | www |
| [#523](https://github.com/kstruck/MMPoolsV3/pull/523) | **T3 + G3** | "Upgrade Now" carries the pool, the add-ons and the coupon. Four independent drops. **G3 pulled forward** (free/locked pools can now be upgraded on /pricing). | 4 | **functions** + www |
| [#524](https://github.com/kstruck/MMPoolsV3/pull/524) | **T6a** | G2 anon auth modal, G4 the live checkout no longer claims it is a sandbox, G5 a completed payment is acknowledged. | 5 | www |
| [#525](https://github.com/kstruck/MMPoolsV3/pull/525) | **T4** | Custom branding is no longer sold. Enforced **server-side** in `computeAddonLines`, so a stale cached bundle cannot be charged either. | 3 | **functions + shared** + www |
| [#526](https://github.com/kstruck/MMPoolsV3/pull/526) | **T1** | Branding colours actually theme the NFL pool. **D4's condition for Monday.** | 2 | www |
| [#527](https://github.com/kstruck/MMPoolsV3/pull/527) | **T5** | A trial unlocks the add-ons the commissioner selected — the reason the AI never appeared to work. | 3 | **functions** |
| [#528](https://github.com/kstruck/MMPoolsV3/pull/528) | **G2 (2nd pass)** | Six more public create CTAs that bounce or lose the post-auth intent. **Found by codex reviewing the flip.** | 1 | www |
| [#530](https://github.com/kstruck/MMPoolsV3/pull/530) | **T9** | The AI Commissioner: persisted feed, real pipeline, member-visible, commissioner-deletable, self-explaining. | **10** | **functions + RULES** + www |
| [#531](https://github.com/kstruck/MMPoolsV3/pull/531) | **T6b** | G7 player estimate required, G9 member-appropriate join-limit copy, G11, G12, G14. | 4 | **functions** + www |
| [#532](https://github.com/kstruck/MMPoolsV3/pull/532) | **T7** | The trial surfaces say what the trial *is*, not only what it costs. | 2 | www |

**37 codex findings were raised across the night. 36 absorbed, 0 rejected, 1
absorbed on corrected reasoning** (T6b r2 — codex's stated mechanism was wrong,
the fix was right for a different reason; recorded that way in the PR).

Root suite: **1762 tests at the start → 2006 at the end.** Functions: 1815.
Emulator rules: 11/11 files, including a new one.

### The findings that would have hurt

Named because they are the argument for the review gate, not decoration:

- **T4 r1** — removing the add-on only from the UI left the backend able to
  price it. This is a CDN-served SPA: browsers hold old bundles, and those
  commissioners would have kept being charged $29 for a feature nothing gates.
- **T9 r3** — the banter fact-builder mapped **field names that do not exist**
  on `StandingsRow`. Every value would have reached Gemini as null, and with
  the no-hallucination rule the feature would have shipped producing empty
  posts. It typechecked, and no unit test would have caught it.
- **T9 r1** — any *participant* could have made the AI publish under its
  identity to the whole pool.
- **The POOLS_OPEN flip r2** — six public create CTAs would have dead-ended
  Monday's invite traffic. That fix had to ship **merged** (#528), because the
  flip PR itself is not merged until Kevin merges it.

---

## 2. NOT DONE — read this part

### T8 (post-wizard branding editor) and T10 (Standings/Results tab merge)

**Not built.** T9 took ten codex rounds — the cap — because it is a rules
change plus a paid-provider path plus two UIs, and rounds 3 through 6 each
found something real. That is where the night went.

With the remaining hours I chose **T6b and T7 over T8 and T10**, a deliberate
deviation from the plan's order. The reasoning: T6b and T7 are launch-facing
friction and copy on the path Monday's commissioners walk, T8 is a new editor,
and T10 is a large refactor that touches the load-bearing published
`offeredTabs` list (HANDOFF K13). Half a T10 would be worse than no T10.

**T8 groundwork is done and the blocker is cleared:** the plan asked whether
the pool-update callable's editability matrix allows `branding.*`. **It does** —
`shared/editability.ts` lists `branding` in `open`, `locked` **and** `archived`.
So T8 is UI-only work, and T1 already shipped the `ColorField` and the live
preview it should reuse.

### The T4 ledger audit — NOT RUN

The plan requires verifying that no real pool ever paid for `customBranding`.
**I could not run it**: this machine has no application-default credentials, no
`gcloud`, and no service-account key.

`scripts/auditCustomBrandingPaid.mjs` is committed and **read-only** (every call
is a `.get()`). Commands are in the chat message.

One finding worth keeping: **the ledger cannot answer this on its own.**
`billing_charges` rows carry `userId / amount / tier / couponCode / sessionId`
and **no add-on breakdown**. The authoritative record is `billing.paid.addons`
on the pool document, so the script reads pools and prints the matching ledger
row for each hit.

**T4's UI removal does not depend on the audit, or on the config save.**

### The Playwright e2e suite is RED — and was already

The instruction was to run the create-pool e2e after T2/T3/T6a. I did. **All 8
specs fail.** I then checked out `925c6d7d` (the pre-session `origin/main`) into
a scratch worktree and ran it there: **also 8/8 red.** Pre-existing, not
regression.

Two distinct causes visible: `admin-claims` cannot find a "System Status"
button, and every `create-pool` spec times out waiting for `Launch pool`. The
suite was last touched **2026-07-04** and **is not run by CI**, so nothing has
been keeping it honest for seven weeks.

I did not fix it. It is a rabbit hole with at least two causes, and the night's
remaining time bought T6b and T7 instead. **It is the first thing I would put
on Sunday** — it is the only automated coverage of the exact flow Monday's
invitees walk, and its absence is why several of tonight's defects reached a
plan instead of a test.

⚠️ **One self-inflicted incident, contained.** To get that baseline I made a
scratch worktree and junctioned `node_modules` into it; `git worktree remove
--force` then deleted the real `node_modules` **through the junction**, in both
the root and `functions/`. Recovered with `npm ci` + `npm --prefix functions
ci`; no tracked file was touched and every suite was re-run green afterwards.
Recorded because it cost ten minutes and the lesson is cheap: **never junction
`node_modules` into a worktree you intend to remove.**

### Not verified in a browser

No screenshots were taken. The dev server needs Firebase auth and a real pool,
and with the e2e suite red there was no cheap way to drive it. **T1 (branding
colours) is the change most worth looking at with your own eyes** after the
Coolify rebuild — the style computation is unit-tested and the application
points are pinned by source invariants, but nobody has *seen* it.

---

## 3. Deviations from the signed plan

1. **G3 shipped inside T3 (#523), not T6a.** Codex found that T3's new
   `?poolId=` links landed on an estimate-only page. Same component, same seam;
   shipping the link without it would promise what the page could not do.
   T6a therefore covered G2, G4, G5.
2. **A second G2 pass shipped as its own PR (#528).** It had to be merged
   separately: the flip is not merged until Monday, so a fix living inside it
   would not be live when the flag goes on.
3. **T6b and T7 were built before T8 and T10**, and T8/T10 were not reached.
   Reasoning above.
4. **T4 clamps server-side**, beyond the plan's "one config save". The plan
   said the UI removal must not depend on the config save; the strongest way to
   honour that was to stop pricing it at the choke point both buy paths use.
5. **T9 reused `pools/{poolId}/messages`** rather than creating a `banter`
   collection. The plan left the shape to the implementer and asked for the one
   that reuses existing read rules; that collection already existed, was
   already ruled, and was already participant-scoped.

---

## 4. Open, and my recommendation

| item | recommendation |
|---|---|
| **#529, the `POOLS_OPEN` flip** | Merge Monday morning, **after** the deploys and the Coolify rebuild, then smoke-test logged out **before** sending invites. It is a build-time constant: merging changes nothing until the rebuild. |
| **T8** | Build it Sunday. It is UI-only, the matrix blocker is cleared, and T1 shipped the components. |
| **T10** | Not before Monday. It touches the published `offeredTabs` list (HANDOFF K13) and the help registry; it wants a full session, not a tired hour. |
| **The e2e suite** | Sunday, before T8. Two causes, seven weeks stale, not in CI. |
| **`billing_config` customBranding `isPremium:false`** | Still worth saving so the admin panel matches reality. **Nothing waits on it** — #525 made the price $0 server-side regardless. |
| **Backlog G6, G8, G10, G13, G15–G20** | Untouched, as the plan says. G8 (the free-plan "10" hardcoded in three places) is the one I would pick up first: T6b had to write copy around it. |
