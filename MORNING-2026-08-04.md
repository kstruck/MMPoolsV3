# MORNING — 2026-08-04

> ⛔ **SUPERSEDED — read `MORNING-2026-08-05.md` instead.** Two of §2's action
> items were done after this was written and this file was never updated:
> **item 1 (lock the HOF spread) is DONE** — measured 2026-08-05, `CAR @ ARI`
> carries `-1.5` with the lock badge closed — and **item 2 (the Coolify
> rebuild) is DONE**, the live bundle having moved to `index-BClXJswC.js`.
> Everything else below stands as the record of 2026-08-04.

**Two days to the HOF game (Thu 2026-08-06, 8:00pm ET, CAR at ARI).** The
full E2E ran and passed; the Thursday-blocking spread lock is still yours
(§2, one click). This is the ONLY morning doc for 2026-08-04.

---

## 0. The one thing worth reading first

**The prod E2E is GREEN: all 45 NFL scenarios passed against production
through the real guarded callables** — including `nfl-pickem-preseason-lifecycle`
(the exact Thursday shape), survivor strikes/rebuys/eliminations, margin, ATS,
ties, cancellations, missed picks and the buyflow. 45 terminal manifests, 45
passed, 0 failed, verified from the `simRuns` manifests, not the UI. Sim
residue after: 0 pools, 0 games. Local suites on the deployed source were green
first (root, functions, emulator incl. the 45-fixture matrix).

The first attempt failed 45/45 against the launch kill-switch (all seven
`poolTypeFlags` are `false` — your "coming soon" posture). I temporarily
enabled ONLY the three NFL season types via the SuperAdmin Settings UI, ran the
suite, and restored all seven to `false`, verified by read-back. #363 exists so
no future E2E needs that round-trip.

## 1. What shipped overnight (all merged; deploy state in §3)

| PR | What |
|---|---|
| #355 | PLAN-IMPORTER-SAFETY (docs only) — merged by Kevin earlier in the evening |
| [#358](https://github.com/kstruck/MMPoolsV3/pull/358) | **One week label everywhere.** Root cause of the Survivor "W2" confusion: the dropdown labels importer week 2 as "Preseason Week 1" while chips/sidebar/banner/standings showed raw numbers — your DEN pick landed on the Aug-13 slate through that trap. `nflWeekLabel`/`nflWeekChip` now the single source (HOF Weekend / Preseason Week N / chips HOF, P1–P3) across every player and admin surface, including the Score confirm you click Thursday. Plus the "Locks In" **live countdown** you asked for (server-synced clock, LOCKED at zero, pings the lock state). Admin week selectors relabel display-only and clamp stale week state on a seasonType switch. 10 codex rounds; qodo re-review Bugs 0. |
| [#360](https://github.com/kstruck/MMPoolsV3/pull/360) | **security-audit gate unblocked.** Three high advisories published overnight (brace-expansion ≤5.0.8 — pinned by our own old override — plus fast-uri and ip-address). Overrides now floor `^5.0.9` in both trees; root `engines: node>=20` declared. |
| [#362](https://github.com/kstruck/MMPoolsV3/pull/362) | **system/config audit trail.** Every write to the kill-switch doc now leaves a `SYSTEM_CONFIG_CHANGED` record in `admin_audit` (uid-attributed, deduped on retries, values redacted structurally, complete changedKeys list). Motivated by tonight's flag flips leaving no trace. 9 codex rounds + qodo 4 bugs absorbed, 5 ✓ Resolved. |
| [#363](https://github.com/kstruck/MMPoolsV3/pull/363) | **Sim-creation bypass** (PLAN-SIM-CREATION-BYPASS.md in-PR). SUPER_ADMIN runs whose payload stamps the `simRunIdForCreate` trust anchor clear the pool-type flag; maintenance mode still blocks everything. codex r1 caught a real P1 in my first draft (payload-keyed bypass would have let an admin mint an unmarked real bracket pool); rekeyed on the stamped anchor. MERGED (`1105392`); qodo's six findings all rejected with written reasoning on the PR (incl. its ask to "fix" the deliberate bracket omission). |
| #348, #359 | **Yours**, merged by you overnight (Production Watchdog; pool-list squares-count fix). Their functions half is deployed (below); their `src/**` half rides the owed rebuild. |

## 2. YOUR Thursday items (unchanged from the earlier runbook, one addition)

1. **Lock the HOF spread** — SuperAdmin → NFL Schedule → Spread Manager →
   Season 2026, Type Preseason, Week 1 ("HOF Weekend" in relabeled UI) →
   Fetch → Lock All Spreads → Save. THE Thursday-blocking item. The lock job
   is dry-run and will not do it for you.
2. **Coolify rebuild** — #358 (src) + #360 (root lockfile) owe ONE rebuild.
   Dashboard link: search HANDOFF.md for "Dashboard, for the next time".
   After it, hard-refresh the Survivor pool: chips read HOF/P1/P2/P3 and the
   countdown ticks under "Locks in".
3. Optional: submit a week-1 Survivor pick before Thu 7:55pm ET, else scoring
   week 1 strikes the entry for non-submission (correct behavior).
4. After the game is FINAL: **Score Week 1 once per pool** (survivor scoring
   is not idempotent — one click).

## 3. Deploy ledger (mirrors HANDOFF's STOP POINT box)

- **Functions: deployed from `1105392` and CERTIFIED** — third run reported
  **175 all `Skipped`, 0 updates, `Deploy complete!`** (fleet 173 → 175 with
  `onSystemConfigWritten` + `getProdWatchdog`). Covers #360's lockfile, #362,
  your #348, and #363.
- **Rules / indexes:** untouched all night. Rules ≡ `0a705c0`.
- **Frontend:** rebuild OWED (§2 item 2). Live bundle unchanged until you
  click.

## 4. Incidents and residuals, stated plainly

- **Main checkout root `node_modules` was broken and is restored.**
  `npx firebase` stopped resolving mid-night; the restoring `npm ci` tripped
  over husky and wiped the folder before failing; `HUSKY=0 npm ci` completed
  and `npx firebase --version` returns 15.25.1. If a deploy misbehaves for
  you, run `HUSKY=0 npm ci` at `D:\march-melee-pools` first.
- **enforceBillingStatus fails every daily run since ~07-22** — missing
  composite index (`billing.status` + `billing.trialEndsAt`). Deliberately
  NOT fixed: the index would silently activate trial-expiry enforcement
  (money, plan-gated). The broken index is accidentally a kill-switch. Your
  Ops Health card surfaces it as the 1 stale job. Decide post-HOF; a
  PLAN-BILLING-ENFORCEMENT with a default-off gate is the shape I'd propose.
- **Two duplicate May-era Pick'em test pools** (`0ybpLzY7fJ3NJbDj0j1l`,
  `xsOlUi1FVOyqHuqgM1Bj`, zero entries each). The admin UI offers only
  hard-delete, which I won't do. Delete them yourself, or leave them — inert.
- **45 orphaned `RUNNING` manifests in `simRuns`** from the kill-switch-blocked
  first E2E attempt. Inert metadata (their runs created nothing);
  `sweepSimRuns` will age them out, or ignore.
- **Your W2 Survivor pick (DEN, Aug-13 slate) is still on your entry** along
  with the new W1 CAR pick. Data was never wrong — the label was. Change the
  DEN pick via the (now honestly labeled) UI if you meant something else.

## 5. Untouched, on purpose

Dependabot (#299–#304 and the new #351–#354), your #348, App Check
(`VITE_RECAPTCHA_SITE_KEY` unset), squares hole, `nflAutoScore`/`statsRecompute`
unset, rescore path and `nflDeepSweep` unarmed, no prod-data writes beyond the
scoped E2E flag round-trip (§0) and the sim harness's own self-cleaning data.

---
---

# ADDENDUM — second overnight session (billing indexes)

**Written by a parallel session** that ran in worktree
`.claude/worktrees/vet-youtube-video-ff1d27` on the task Kevin assigned
separately: fix the `enforceBillingStatus` missing index. Everything above is the
other session's and is unmodified.

⚠️ **The two sessions reached OPPOSITE conclusions on the billing index, and you
should decide with both in front of you.** §4 above says it was deliberately NOT
fixed ("the broken index is accidentally a kill-switch... decide post-HOF").
Kevin explicitly asked this session to fix it and open a PR. **Both are right
about the risk**; the disagreement is only about whether the fix should exist as
an unmerged PR. It does now, and **it is not deployed** — which preserves exactly
the safety §4 was protecting.

> ⚠️ **HISTORICAL from here to the end of ADDENDUM 1.** Both halves of that
> sentence have since changed: #365 is MERGED and its indexes are DEPLOYED
> (verified 2026-08-05). **§B0 in ADDENDUM 2 is the current state.**

## A1. PR #365 — the two indexes ⬅️ HISTORICAL (as of 2026-08-04)

> ⚠️ **THIS SECTION IS OUT OF DATE AND IS KEPT AS A RECORD.** It describes #365
> as open, unmerged and undeployed, which was true when it was written. Since
> then: **#365 MERGED** (`f27c8ec`, 2026-08-04), and **the indexes were deployed
> 2026-08-05 and verified present in prod** — see **§B0**, which is the current
> state. Read §B0 first; nothing in A1 should drive an action today.

<https://github.com/kstruck/MMPoolsV3/pull/365>

> ⚠️ **The PR carries ONE named open finding: `enforceBillingStatus` has no
> kill-switch.** It is deferred, not resolved, and the deferral is deliberate —
> see A1b. Nothing else is outstanding.

**Correction to §4 above, offered gently because it changes the diagnosis.** §4
says the job "fails every daily run since ~07-22". The evidence says it has
**never** worked:

```powershell
git log -S 'billing.trialEndsAt' -- firestore.indexes.json   # returns NO commits
```

The index was never declared in the repo at all. Two readings fit both facts:
either the job simply never worked and ~07-22 is when the heartbeat started
reporting it, or **an index created by hand via the console link in the
`FAILED_PRECONDITION` error was later DELETED by a `firestore:indexes` deploy** —
that command reconciles prod against the file and offers to remove anything not
in it. The second is a hypothesis, not a finding; it is worth stating only
because it is a live hazard on this very deploy (see the warning in
`PLAN-BILLING-INDEX-DEPLOY.md`).

**`PLAN-BILLING-INDEX-DEPLOY.md` is in the PR** and carries the pre-deploy
population count, the reconcile-deletion warning, monitoring reads and a rollback
table. Read it before running anything.

### 🔑 The finding that directly answers §4's proposal

§4 proposes "a PLAN-BILLING-ENFORCEMENT with a default-off gate". **That gate does
not exist today, and `enforceBillingStatus` is the only risky scheduled job in the
repo without one.** Measured:

```powershell
grep -rln "readJobGate|doc\('system/config'\)" functions/src --include=*.ts
```

returns **eleven** files — including `stripe.ts` and `monetizationAlerts.ts`, the
other two money paths. **`billing.ts` is not among them.** There is no `enabled`
and no `dryRun` anywhere in the job (`billing.ts:81-125`); it reads
`settings/billing_config` only for `gracePeriodDays`/`trialDays`.

So the job writes on its first successful run or not at all. **A `dryRun` would
have turned this from an irreversible all-or-nothing into: deploy the index, read
`trialToGrace` off the heartbeat next morning, then decide.**

## A1b. THREE independent reviewers converged on the kill-switch — and it is the PR's one open finding

This is the strongest signal of the night:

1. **The other overnight session** (§4 above) — *"a PLAN-BILLING-ENFORCEMENT with
   a default-off gate is the shape I'd propose."*
2. **This session**, independently, from the eleven-vs-`billing.ts` measurement
   above, before qodo ran.
3. **qodo**, on the post-rebase re-review, raised it unprompted as a **⭐ New**
   finding: *"the job has no system/config enabled gate and no default-on dry-run
   mode... increases the risk of unintended large-scale state transitions/emails
   on first successful run."*

**It is DEFERRED, not fixed, and #365 carries it as a named open finding** — per
CLAUDE.md §2c, a PR that stops with findings open must say so rather than go
quiet. Three reasons, all of which point the same way:

- **Scope.** The task was: add the indexes, run the gates, stop before deploying.
- **It would make YOUR deploy riskier.** #365 touches no `functions/` code, so it
  owes **only** an index deploy. Adding a gate to `billing.ts` would owe a **full
  functions deploy** two days before the HOF game — trading a narrow reversible
  change for a broad one.
- **It is genuinely plan-gated.** Changing `billing.ts` behaviour trips the money
  trigger in `mmp-change-control` §1 *directly*, not adjacently. That earns a real
  PLAN + review log + sweep, not a finding absorbed at 2am on another PR.

**Recommendation: build the gate as its own change post-HOF, then deploy the
index behind it in dry-run first.** That ordering gets you the counts before any
pool moves — which is the thing nobody can currently have.

⚠️ qodo's own precedent data notes the team *"has repeatedly rejected adding
kill-switch enabled gates as overkill/out-of-scope"* (#205, #190, #183). That is
**not** why it was deferred — the gate is warranted here — but it does mean this
is your call, not a mechanical fix.

## A2. Corrections to what this session initially concluded

Recording these because the first draft of this addendum asserted them and the
other session's doc disproved them. Both were wrong in the *safe* direction, but
wrong.

- ❌ **"All seven pool types are disabled — recommend turning them back on."**
  Withdrawn. §0 above establishes it is Kevin's deliberate **"coming soon"
  posture**. This session also mistook the eight `SYSTEM_CONFIG_CHANGED` audit
  entries at 01:03 and 01:10 for someone clicking toggles — they are the other
  session's E2E flag round-trip (enable 3 NFL types → run suite → restore all
  seven), which §0 documents and verified by read-back.
- ❌ **"There is no manual path to lock spreads."** Wrong. A grep for callers of
  `lockSpreadsOnce` found only the scheduled job, but the admin UI writes spreads
  directly: `src/components/admin/SuperAdminNFLSpreads.tsx:184` is the "Lock All
  Spreads" button, and it is §2 item 1 above — your Thursday-blocking click.

## A3. Findings that stand, and are not covered above

**1. The "coming soon" posture does not actually hide anything.**
`src/components/PoolTypeGate.tsx` has **zero call sites** — its only reference in
the repo is its own definition. Grepping
`isPoolTypeEnabled|poolTypeFlags|resolvePoolTypeFlags` across `src/**/*.tsx`
returns **no hit outside `SuperAdmin.tsx`**, the panel that writes the flags. So
no user-facing surface consults them.

Consequence: the admin panel claims *"Disabling a type hides its card on
`/create-pool`"* — it does not. Read off prod, every type still advertises a live
CTA (`SETUP SQUARES`, `SETUP PICK'EM`, `SETUP SURVIVOR`, `SETUP MARGIN`,
`CREATE PROPS POOL`, plus Bracket and Playoff cards). Users complete the whole
wizard and are rejected at submit by `assertPoolCreationAllowed`. **That is the
Watchdog's 45 client errors** — not people probing a disabled feature, but people
finishing a flow the UI told them was open. Fixing this is a `src/**` change and
owes a Coolify rebuild, so it was not done unattended.

**2. A third missing composite index, unrelated to billing.**
`functions/src/aiCommissioner.ts:45` queries `pools/{id}/audit` with
`severity in [...]` + `orderBy('timestamp','desc')` — needs a composite index that
does not exist. It is on `onWinnerUpdate`, a SQUARES-only trigger. Left out of
#365 to keep that PR to its assigned scope. Ready to paste:

```json
{
  "collectionGroup": "audit",
  "queryScope": "COLLECTION",
  "fields": [
    { "fieldPath": "severity", "order": "ASCENDING" },
    { "fieldPath": "timestamp", "order": "DESCENDING" }
  ]
}
```

**3. `deploy:backend` was shipping two of three backend surfaces** — it was
`firebase deploy --only functions,firestore:rules`, so declaring an index and
releasing through it created nothing. It also used a bare `firebase` (against the
repo's `npx` convention) and omitted `--project`, which matters a lot more once
the script deploys indexes, because an index deploy reconciles and can **delete**.
All three fixed in #365, and the preferred ritual in `mmp-change-control` had the
same missing third surface — also fixed. This is a plausible mechanism for how a
declared index could go missing in the first place.

**4. No other scheduled job has a missing index.** Every
`db.collection(...).where(...)` chain in `functions/src` and every modular-SDK
`query(...)` in `src` was scanned and triaged against the 15 existing indexes.
`autoLockPools`, `nflFinalizeSweepJob`, `syncGameStatus` and `runReminders` are
all covered; seven other hits were same-field range pairs the automatic
single-field index serves. Consistent with Ops Health showing exactly one stale
job.

**5. The `SYSTEM_CONFIG_CHANGED` audit payload is invisible in the UI.** #362
computes a before/after diff (`lib/configDiff.ts`), but the Operations table
renders only timestamp / actor / action / target / ok. The trail cannot answer
the question it was built for. Read raw at
<https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/data/~2Fadmin_audit>.

**6. There is no admin UI for the NFL job gates.** System → Settings renders only
Maintenance Mode, Auto-Close Sweep and Pool Type Availability — nothing for
`nflSpreadLock`, `nflAutoScore` or `nflLockWatch`, and no display of their state.
Arming any of them is a Firebase-console data edit.

**7. One unidentified flaky test.** The first `npm --prefix functions test` run
reported `1 failed | 1333 passed`; five subsequent runs were all `1334 passed`.
The command truncated output so the failing test's name was never captured. It
did not reproduce. Flagged rather than called clean.

## A4. Gate evidence for #365

- root vitest **623/623** (45 files) · functions vitest **1334/1334** (97 files)
- `tsc --noEmit` clean · `eslint .` **0 errors** · functions build · frontend build
- **codex: 5 rounds.** R1 P1 absorbed → R2 clean → self-review found stale skill
  quotes → R3 P1+P3 absorbed → R4 clean → R5 clean on the post-qodo diff.
- **qodo: REPORTED, 9 findings — 5 absorbed, 4 rejected with reasoning on the PR.**
  Its best finding was the missing `--project`, which was risk the PR itself
  introduced. ⚠️ The watcher's first pass returned `QODO PARTIAL` with **0**
  inline findings; the settled re-arm found **5**. The settle floor is the only
  reason the gate was not closed against an unfinished report.
- **Rebased onto `origin/main` after #364 landed**, so this branch contains only
  its own commits and does not touch the doc above.

✅ **CI: all 7 checks pass on the final rebased head** — `build-and-test`,
`emulator-tests`, `lint`, `security-audit`, `nginx-validate`, `CodeQL`,
`Analyze`. (An earlier revision of this addendum warned CI had not re-run; that
was true only of the two docs-only commits, which the workflow path-filters. The
rebased head ran the full suite.)

**PR state:** `MERGEABLE`, 6 commits, rebased onto `origin/main` after #364.

---
---

# ADDENDUM 2 — third overnight session (buy-flow dead end, hosting banner)

⚠️ **This is the SECOND addendum on this file.** The body above is the first
overnight session; the unnumbered "ADDENDUM" above it is the billing-index
session, whose PR #365 is now **MERGED** (`f27c8ec`). ❌ **Its indexes are NOT
deployed** — an earlier revision of this sentence said they were, and §B0 below
disproves it with the job's own error. This is a third session, run after that
merge. It appends rather than competing, per the collision that cost a rebase
the night before.

> # ⬛ THE ONE THING YOU STILL HAVE TO DO
>
> **Trigger a Coolify rebuild, then look at two pages.** Full steps in §B3.
>
> Everything else from this session is merged. The three PRs (#367, #368, #366)
> are in `main`; #354 and #353 are closed. **None of it reaches production until
> the frontend is rebuilt** — that is a click in a dashboard I have no access
> to, so it is the single remaining action.
>
> After that, two optional-but-recommended items: check the billing job's
> heartbeat (§B0 — its first-ever post-index run has now happened) and decide
> whether to arm auto-scoring (§B2).

⚠️ **HISTORICAL NOTE, kept because it explains a confusing hour.** As originally
written, this addendum said "nothing in this session was merged, deployed,
rebuilt, or flipped; two PRs are open and waiting on you". That was true when it
was written — but the addendum itself lived on an **unmerged branch**, so Kevin
opened `MORNING-2026-08-04.md` on `main`, saw the 297-line version without any of
this, and reasonably reported there was nothing for him to do. **A handoff
document that ships inside the PR it describes is invisible to the person it is
addressed to.** Worth remembering: land the doc first, or say up front where it
lives.

## B0. TASK 0 — ✅ CLOSED: the index was never deployed, and now it is

> # ✅ RESOLVED 2026-08-05 04:1x UTC — KEVIN DEPLOYED THEM. BOTH NOW EXIST IN PROD.
>
> ("exist", not "are live" — the CLI cannot report `READY` vs `CREATING`, and
> the caveat further down says so. The heading should not claim more than the
> body can support.)
>
> **Measured, not assumed.** `npx firebase firestore:indexes --project
> gridiron-gamble-uzuqo` now returns **17** indexes (was 15), and the two that
> were missing are among them:
>
> ```
> pools | COLLECTION | billing.status + billing.trialEndsAt       + __name__
> pools | COLLECTION | billing.status + billing.gracePeriodEndsAt + __name__
> ```
>
> The deploy printed `✔ firestore: deployed indexes in firestore.indexes.json
> successfully` and **offered no deletions** — so the reconcile hazard the
> runbook warns about did not fire, and no untracked index was removed.
>
> ⚠️ **UNVERIFIED: build STATE.** `npx firebase firestore:indexes` lists indexes that
> EXIST; it does not report `CREATING` vs `READY`, and `gcloud` is not installed
> here to ask. On a 118-document collection a build is seconds rather than
> minutes, so this is very likely `Enabled` already — but the only proof is the
> job itself.
>
> ### What to check, and when
>
> **The deploy does NOT run the job.** `enforceBillingStatus` is
> `onSchedule('0 23 * * *', America/New_York)`, so the next attempt is **23:00 ET
> on 2026-08-05**. Ops Health will keep showing the old failing beat until then;
> that is expected and is not a failure signal.
>
> The morning after:
>
> 1. SuperAdmin → Overview → **Ops Health**.
> 2. ✅ **Win condition: no `enforceBillingStatus` line under the tile.**
>    ⚠️ Judge on that line, **not** on the count. `STALE JOBS` aggregates every
>    job in `SCHEDULED_JOB_EXPECTATIONS` (`lib/heartbeat.ts:228`), so any
>    unrelated stale job keeps it above zero and would make a successful billing
>    repair read as a failure. It happened to be 1-and-only-billing all week,
>    which is what made the shorthand tempting.
> 3. ❌ Still `FAILED_PRECONDITION` → the index is real but not READY yet; check
>    the console's Indexes tab for `Building`.
> 4. ❌ A *different* error → new, and it is a money path. Send it to me.
> 5. For the counts, read `system/heartbeats` in the console (no UI renders it):
>    ⚠️ **`failedTransitions` is ABSENT on a healthy run — do not go looking for
>    a zero.** `billingEnforceVerdict` (`functions/src/lib/heartbeatVerdicts.ts:119-133`)
>    puts `failedTransitions` in the detail **only when it is above zero**, and
>    sets `ok: false` with an `error` string alongside it. So:
>    **healthy = the field is not there**, and the beat carries just
>    `trialToGrace` and `graceToLocked`. **Seeing `failedTransitions` at all is
>    the bad case.**
>
> **Expect commissioner emails on the first successful pass** — roughly four
> trial pools, all yours, moving `trial → grace_period`. Nothing can lock that
> night: no pool currently sits in `grace_period`, so phase 2 has no candidates
> for another seven days. That week is the window to build the §B5 gate.
>
> ⬇️ **HISTORICAL — the 03:43 UTC reading that prompted the deploy.**

> ## ❌ 2026-08-05 03:43 UTC — IT FAILED AGAIN. THE INDEX WAS NOT LIVE.
>
> **The first post-index run has now happened and it threw the SAME error.**
> Read off the SuperAdmin Ops Health card at 03:43 UTC:
>
> ```
> STALE JOBS: 1
> enforceBillingStatus — failing 44m ago — 9 FAILED_PRECONDITION:
>   The query requires an index. …create_composite=ClNwcm9qZWN0cy9ncmlkaXJvbi1nYW1ibGUtdXp1cW8v…
> ```
>
> 44 minutes before 03:43 UTC is **02:59 UTC = 23:00 ET on 2026-08-04** — the
> job's nightly slot, and the first run after #365 merged. Decoding the
> `create_composite` blob in that error names the index it wants:
>
> ```
> projects/gridiron-gamble-uzuqo/databases/(default)/collectionGroups/pools/indexes/_
>   billing.status + billing.trialEndsAt + __name__
> ```
>
> — which is **exactly** the first of the two indexes `firestore.indexes.json`
> has declared since #365 merged (verified on `main`: both `pools` composites are
> present, `COLLECTION` scope).
>
> ⚠️ **So the handoff claim that "the indexes are DEPLOYED / both indexes are now
> live in prod" was WRONG.** The declaration landed; the deploy did not take.
> ⚠️ **UNVERIFIED — three candidate causes, none of them checked.** Reading the
> Firebase console needs an auth token this session is not permitted to extract,
> so the cause is a hypothesis in every case below; only the FAILURE is measured.
> Do not repeat any of these three as established:
> **(a)** `npx firebase deploy --only firestore:indexes` was never actually run;
> **(b)** it ran but the index is still `Building` — unlikely after ~19 h on a
> 118-document collection, but it is the one benign explanation;
> **(c)** it ran against a different project.
>
> **What this costs while it stays broken:** expired trials never move to grace
> period and expired grace periods never lock. No pool is harmed — the job fails
> before its first write — but the enforcement side of billing does not exist.
> There is also a silver lining worth stating: this is why nothing has been
> auto-locked or auto-emailed by surprise.
>
> ### ⚠️ FIRST, A DECISION — deploying this index ARMS live billing enforcement
>
> ✅ **DECIDED AND DONE 2026-08-05: Kevin deployed it.** The section below is
> kept because it is the record of what that decision accepted.
>
> This is not a neutral repair, and an earlier revision of this section told you
> to just run the deploy. codex caught that, and it was right: the moment the
> index works, the job starts doing the thing it has never once done.
>
> **What the next 23:00 ET tick does, with no gate and no dry-run** (there is
> neither — §B5 is the plan to build them):
>
> | Phase | Effect | Reversible? |
> |---|---|---|
> | trial → grace_period | sets `gracePeriodEndsAt` = now + **7 days**, emails the commissioner "pool locks in 7 days" | status yes, **email no** |
> | grace_period → locked | pool renders read-only behind a lock modal **for every participant**, emails the commissioner | status yes, **email no** |
>
> **Blast radius, measured 2026-08-04:** five pools carried
> `billing.status == "trial"` and **all five are yours**; one
> (Kevin Struck's 2026 NFL Survivor Pool) was set to `active` to immunise it, so
> roughly **four** would transition. `BUYFLOW TEST` is deliberately among them.
> Nothing carries a `grace_period` status today, so **phase 2 cannot fire on the
> first night** — the earliest anything locks is 7 days after phase 1 runs.
>
> ⚠️ **UNVERIFIED — that census is from 2026-08-04 and nobody has re-run it.**
> If a real commissioner has started a trial since, they are in scope too.
>
> **Recommendation: deploy the index.** Phase 1 is bounded, it is your own
> pools, the emails are ones you would have wanted to receive, and phase 2 is
> seven days away — which is more than enough time to build the gate in §B5
> before anything locks. The alternative (build the gate first, deploy it
> default-off, then the index) is stricter and is what
> `PLAN-BILLING-ENFORCEMENT` §4 argues for in general; it costs a functions
> deploy in HOF week.
>
> **If you would rather not have ANY pool transition yet, do not run the deploy
> below** — the enforcement side has been dead since ~2026-07-22 and one more
> week changes nothing.
>
> ### Then fix it — before the Coolify rebuild, it is faster
>
> 1. In `D:\march-melee-pools`:
>
> ```bash
> git pull
> ```
>
> 2. Deploy the indexes on their own (this surface is independent of functions
>    and rules ordering):
>
> ```bash
> npx firebase deploy --only firestore:indexes --project gridiron-gamble-uzuqo
> ```
>
> 3. ⚠️ **READ THE PROMPT BEFORE CONFIRMING.** An index deploy RECONCILES
>    production against the file: any index that exists in prod but is NOT in
>    `firestore.indexes.json` is offered for DELETION. If it offers to delete
>    anything, say **no** and tell me what it named — that is a real finding.
> 4. **What you should see:** `✔  firestore: deployed indexes in firestore.indexes.json successfully`.
> 5. Then open the Firestore console → **Indexes** tab:
>    <https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/indexes>
>    Look for two `pools` composites — `billing.status ↑ billing.trialEndsAt ↑`
>    and `billing.status ↑ billing.gracePeriodEndsAt ↑`. **Both must read
>    `Enabled`.** `Building` means wait; absent means the deploy did not take and
>    something is wrong with the project/CLI auth.
> 6. ⚠️ **Deploying the index does NOT run the job.** `enforceBillingStatus` is
>    `onSchedule('0 23 * * *', America/New_York)`, so the next attempt is
>    **23:00 ET on whatever day you deploy** — and if you deploy after 23:00,
>    the night after that. Ops Health will keep showing the old failing beat in
>    the meantime; that is expected and does NOT mean the deploy failed.
>    Check the morning after the next tick. The win condition is **no
>    `enforceBillingStatus` line** under the Stale Jobs tile — not the count
>    itself, which aggregates every scheduled job.
>
> ⚠️ **When it does finally run, it runs LIVE** — no kill-switch, no dry-run
> (§B5). Expect commissioner emails on the first successful pass.

⬇️ **Everything below is the 2026-08-04 09:18 UTC snapshot**, kept because it is
the evidence that the failing beat pre-dated the fix rather than being caused by
it.

**The answer at the time was neither "clean" nor "failed". The job had not run
since the indexes were deployed**, because the handoff into that session assumed
it would have and the clock said otherwise.

**Measured on production at 09:18 UTC on 2026-08-04:**

- SuperAdmin → Overview → **Ops Health** still shows `STALE JOBS: 1`, and it is
  still `enforceBillingStatus — failing 6h ago — 9 FAILED_PRECONDITION: The
  query requires an index.`
- 09:18 UTC minus 6 h = **03:00 UTC** = **23:00 ET on 2026-08-03** — exactly the
  job's nightly slot (`{ schedule: '0 23 * * *', timeZone: 'America/New_York' }`,
  `functions/src/billing.ts:86`).
- PR #365 merged at **08:53:53 UTC** (`git log -1 --format=%cI f27c8ec` →
  `2026-08-04T02:53:53-06:00`).

So the failing beat is **5 h 53 min OLDER than the fix**. It is the last
pre-index run, not a post-index failure. `failedTransitions` is not the question
yet — that run never got past its first query.

**The first post-index run was 23:00 ET on 2026-08-04 (03:00 UTC on 08-05) —
which has now happened, unread (see the box at the top of this section).** Check
it:

1. Go to <https://www.marchmeleepools.com/super-admin> and stay on the
   **Overview** tab.
2. Find the **Ops Health** card — it sits between "API Status Center" and
   "Systems Monitor".
3. **What good looks like:** no `enforceBillingStatus` line under the Stale Jobs
   tile. ⚠️ The COUNT is not the signal — it aggregates every job in
   `SCHEDULED_JOB_EXPECTATIONS`, and an unrelated stale job would hold it above
   zero while billing is perfectly healthy. Billing happened to be the only
   stale job all week, which is why "0" read as sufficient.
4. **If it still says `failing` with `FAILED_PRECONDITION`:** the index deploy
   did not take. Re-run it — `npm run deploy:backend` now includes
   `firestore:indexes` (that was part of #365) — and check the Firebase console
   → Firestore → **Indexes** tab for two entries on `pools`:
   `billing.status + billing.trialEndsAt` and
   `billing.status + billing.gracePeriodEndsAt`, both **Enabled**, not
   **Building**.
5. **If it says `failing` with a DIFFERENT error:** that is new, and it is a
   money path. Send me the message.
6. **For the counts** there is still no UI — `system/heartbeats` is console-only
   (HANDOFF known-open item 4). Read it at
   <https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/data/~2Fsystem~2Fheartbeats>
   and screenshot the `enforceBillingStatus` map. **Healthy is
   the ABSENCE of `failedTransitions`** — `billingEnforceVerdict` omits that
   field entirely unless it is above zero (`heartbeatVerdicts.ts:119-133`), so a
   healthy beat carries only `trialToGrace` and `graceToLocked`. Seeing
   `failedTransitions` at all is the bad case. `trialToGrace` should be small (a
   handful of pools at most).

   ⚠️ **`trialToGrace` is a TRANSITION count, not an email count, and the two
   are not the same number.** The handoff into this session said they were
   equal; they are not. `trialToGraceCount++` fires immediately after the pool
   update (`functions/src/billing.ts:145`), and only then does the email get
   attempted — inside a nested `try/catch` that logs and continues on failure,
   and skipped entirely when `resolveCommissionerEmail` finds no address on
   either the pool or the owner record. So `trialToGrace` is an **upper bound**
   on emails sent. To know what actually went out, read the function logs for
   `Grace-period warning email queued for pool` and
   `No commissioner email found for pool`. There is no separate delivery
   counter on this job — `runReminders` has one, this does not, and adding one
   would fold naturally into the gate work in §B5.

⚠️ **When it does run, it runs LIVE.** There is no kill-switch and no dry-run on
this job — see §B5.

## B1. What shipped — ✅ BOTH MERGED

> ✅ **UPDATED 2026-08-05 03:1x UTC.** Kevin read this section, said "go with
> your recommendation and do what needs to be done", and both PRs were merged on
> that instruction. **Everything in §B1 and §B4 is now DONE except the Coolify
> rebuild**, which is the one action that is still his — it is a click in a
> dashboard I have no access to.
>
> | PR | State | Squash commit |
> |---|---|---|
> | #367 buy-flow quote dead end | **MERGED** | `3e11017` |
> | #368 hosting-fees-paid banner | **MERGED** | `7da9189` |
> | #366 dependency group (11 minor/patch) | **MERGED** | `eacfc37` |
> | #354 lucide-react 1.x | **CLOSED** (major, build fails) | — |
> | #353 @vitejs/plugin-react 6 | **CLOSED** (major, 4 checks fail) | — |
>
> ⚠️ **NONE of it is live yet.** All three are `src/` or lockfile changes and the
> production bundle does not move until the frontend is rebuilt. **§B3 is the
> only thing left to do.**

Both are `src/` only: **no functions deploy, no rules, no indexes.** Both owe a
Coolify rebuild (§B3).

The rest of this section is the original write-up, unchanged, because the
reasoning is why they were worth merging.

### PR #367 — the buy flow priced every pool at $0 and disabled the only button

<https://github.com/kstruck/MMPoolsV3/pull/367>

**This is the one that matters.** It is not the cosmetic task it was queued as.
While reproducing the dead "Activate Pool (Free Allocation)" button I found the
cause, and it is bigger: **every hosting quote in the app has been failing, and
the failure rendered as the word FREE.**

`dbService.getPoolQuote` passed its params straight to the callable. Both call
sites set `couponCode: <value or undefined>`, and the Firebase callable
serializer encodes a **present key with an `undefined` value as `null`** on the
wire. The server schema is `.optional()`, which accepts *absent* and rejects
`null`. **Every quote request made without a coupon failed** — which is the
default path for every user.

It never surfaced because the card derives every figure with `?? 0`, and its
catch block deliberately stays silent unless a coupon was typed. "Price unknown"
and "price is zero" were literally the same render.

**Measured on production, on your own `BUYFLOW TEST` pool** (`/pricing`, pool
selected):

- `Base Hosting fee (25 estimated players)` → **FREE**
- `UPGRADE PREMIUM TOTAL` → **FREE**
- `ACTIVATE POOL (FREE ALLOCATION)` in full-brightness brand red with a normal
  cursor, and `button.disabled === true` read straight off the live DOM
- SuperAdmin → Production Watchdog, **50 client errors in 24h**:
  `medium: Invalid request: couponCode — Invalid input: expected string, received null`
  (`medium` identifies the caller — `getPoolQuote` logs at MEDIUM,
  `createCheckoutSession` at HIGH)

The repo already knew about this serializer behaviour and had fixed it in exactly
one place: `createCheckoutSession` carried the strip, with a comment naming
`couponCode`. `getPoolQuote`, added in the same overhaul against the same field,
never got it.

The dead free-allocation button was the **second** defect, and it is the one you
queued. The two ship together deliberately: fixing the button alone, while the
quote was broken, would have let a commissioner activate a **paid** pool for
nothing, in one click, on every pool in the system.

- Plan gate taken (money trigger): `PLAN-BUYFLOW-QUOTE-DEADEND.md` plus
  `PLAN-BUYFLOW-QUOTE-DEADEND-REVIEW-LOG.md`
- 34 new tests, root suite 623 → 658; functions 1334/1334; `tsc -b` clean; lint
  0 errors; build succeeds; **all 7 CI checks pass**
  — **all counts measured 2026-08-04** (a bare number in a doc reads as current
  forever; `mmp-docs-and-writing` requires the date)
- **codex 5 rounds, 5 findings, all absorbed** — rounds 2 and 3 each found a
  defect in the previous round's fix, exactly as CLAUDE.md §2c predicts
- **qodo REPORTED, 2 findings, both absorbed.** Its finding was the sharpest of
  the whole review and neither codex nor I caught it: base and total were made
  honest, but the four add-on rows and the discount row kept printing `+$0.00`
  beside the "Pricing unavailable" notice — and the **"Free Pool Exempt" badge**
  keyed on `basePrice === 0`, so on every failed quote it asserted a paid pool
  was free-tier exempt

### PR #368 — "hosting fees paid" banner for the pool manager

<https://github.com/kstruck/MMPoolsV3/pull/368>

Your request. `active` was the only billing status `BillingGate` rendered
nothing for — it short-circuited at the top of the component *alongside "no
billing record at all"*, so a commissioner who had paid got the same blank
treatment as a pool that never had a billing document. Green background, white
text, commissioner-only, structure matching the four sibling banners in the same
file.

**The call you asked me to make, and the flag.** The condition is `active` **AND
a settled paid path** — not `active` alone:

| Pool | banner? | copy |
|---|---|---|
| Stripe purchase | yes | `$29.00 paid. Nothing further is due.` |
| 100%-off coupon | yes | `with a pool credit or promotion` |
| Pool Credit redemption | yes | `with a pool credit or promotion` |
| **Free allocation** | **no banner** | — |
| Legacy pool, `tier` missing | no banner | — |
| Paid tier, `pricePaid` missing | yes | `This pool is fully activated. Nothing further is due.` |

A free-allocation pool is `active` with `tier: 'free_tier'` and `pricePaid: 0`
(`functions/src/stripe.ts` writes exactly that on the $0 path). Telling that
commissioner their hosting fees are paid is a fabricated claim on a money
surface. The discriminator is the **tier plus `paidVia`**, not `pricePaid`,
because a Pool Credit or a 100%-off coupon also leaves `pricePaid` at 0 while
the commissioner genuinely settled.

⚠️ **YOUR CALL:** a genuine free-allocation pool therefore gets **no** banner. If
you want one there it needs different words — *"hosting is free for this pool"*,
not *"fees paid"*. Say so and it is a one-line addition.

- 10 new tests, billingGate suite 15 → 25, root suite 623 → 633
  (**measured 2026-08-04**)
- **Mutation-tested, 6 applied, 6 killed**, each by the case written for it
- **codex 4 rounds** — round 1 clean, then *self-review* found that
  `tier !== 'free_tier'` is TRUE for a missing tier (a legacy pool would have
  been told its fees were paid on no evidence); round 2 found that
  `redeemPoolCreditForPool` never touches `tier`, so a credit-activated pool
  sits on `free_tier`; round 4 clean
- **qodo REPORTED, 3 findings — 1 absorbed, 2 rejected with reasoning on the PR**
  (a 200–300 ms animation rule that does not exist in this repo and would have
  made the new banner the only one of five that animates differently; and emoji
  in a `vi.mock` test double, where all five pre-existing icon mocks are emoji)
- **all 7 CI checks pass**

### How they were merged — ✅ DONE, recorded for the audit trail

Merged 2026-08-05 on Kevin's explicit instruction, in this order, each squashed
with the branch deleted:

```
gh pr merge 367 --squash --delete-branch    ->  3e11017
gh pr merge 368 --squash --delete-branch    ->  7da9189
gh pr merge 366 --squash --delete-branch    ->  eacfc37
```

All three reported `MERGEABLE / CLEAN` immediately before merging, and all three
carried 7/7 green CI. No conflicts. `origin/main` moved
`f27c8ec` → `3e11017` → `7da9189` → `eacfc37`.

⚠️ **Your local checkout at `D:\march-melee-pools` will not have any of this
until you run `git pull`.** That is also why this addendum looked empty when you
first opened it — it was on an unmerged branch.

## B2. AUTO-SCORING — the full arming runbook

> Your words: *"I expect all games to be auto scored, nothing should be manual.
> The pool manager should never have to do anything manual."*

**Everything below is a config flip in `system/config`, which is yours to make,
not mine.** Every value and line number was verified against the source tonight.

### What is already on, with no switch at all

`syncNFLScoresJob` (`functions/src/nflSchedule.ts:422`, schedule `*/5 * * * *`)
has **no config gate**. It pulls ESPN scores into `nfl_games` every five minutes,
unconditionally, and it is already running. That is the feed. It does not score
pools.

### The three switches, and what each one does

| Config key | Job / heartbeat key | Schedule (verified) | What arming it does |
|---|---|---|---|
| `nflAutoScore` | `nflAutoScoreJob` | `*/10 * * * *` ET | Scores pools from the synced games — the thing you actually want |
| `nflFinalize` | `nflFinalizeSweepJob` | `30 4 * * *` ET | Finalizes season-complete pools the inline check missed |
| `nflSpreadLock` | **`lockNFLSpreadsJob`** | `0 9 * * 2` ET (Tuesdays 9 am) | Locks spreads for the coming week |

⚠️ **The spread-lock job's CONFIG key and its HEARTBEAT key are different
words.** The config is `system/config.nflSpreadLock`; the job and its
`system/heartbeats` entry are **`lockNFLSpreadsJob`**
(`functions/src/nflSchedule.ts:999,1002`, and the key in
`SCHEDULED_JOB_EXPECTATIONS`, `lib/heartbeat.ts:251`). Looking for an
`nflSpreadLockJob` heartbeat will find nothing. The other two match.

⚠️ `nflAutoScoreJob` runs **every 10 minutes**, not every 5 — verified at
`functions/src/nflAutoScore.ts:653`. The 5-minute job is the score *feed*.

Target values:

```
nflAutoScore  = { enabled: true, dryRun: false }
nflFinalize   = { enabled: true, dryRun: false, liveSeasonTypes: [1] }
nflSpreadLock = { enabled: true, dryRun: false }
```

### ⚠️ `nflFinalize` will SILENTLY REFUSE to arm without `liveSeasonTypes`

`readSweepGate` (`functions/src/nflFinalize.ts:375-395`) returns dry-run when
`dryRun: false` is set but `liveSeasonTypes` is missing or empty, and logs
`[nflFinalizeSweep] arm request refused; continuing dry.` (`:458`). It does not
error, and the config still reads `dryRun: false` — so **it will look armed and
do nothing.** `[1]` means preseason only; **you must widen it before the regular
season** (`[1,2]`, or `[2]` once preseason is over).

`nflAutoScore` and `nflSpreadLock` use the plain `readJobGate`
(`functions/src/nflSchedule.ts:911`) and have no such second key.

### The recommended sequence — dry-run first

⚠️ **THE SUPERADMIN UI CANNOT DO THIS. Use the Firebase console.** The System
tab writes a small, hardcoded subset of `system/config` — one control per key,
no generic editor. Every `settingsService.update` call in
`src/components/SuperAdmin.tsx` writes one of: `enableBracketPools` (`:2980`),
`maintenanceMode` (`:3002`), `tickerDurationSec` (`:3027`), `autoClose`
(`:3045,3055,3058`), `poolTypeFlags` (`:3083`), plus `loyaltyTiers` (`:275`) and
`propCategories` (`:560`) from other tabs. **There is no editor for
`nflAutoScore`, `nflFinalize` or `nflSpreadLock` anywhere in the admin
surface**, and that is the part that matters here.

⚠️ Two corrections are baked into that paragraph, both from reviewers. An
earlier draft sent you to the System tab to find a control that does not exist
(codex). Its replacement then claimed the tab edits "exactly three things", which
is also false — it misses `enableBracketPools` and `tickerDurationSec` (qodo).
The enumeration was written from a grep filtered on the keys I was already
looking for, which is precisely how it missed the rest. The negative claim above
is the load-bearing one and it survived both. Building those toggles is a
worthwhile follow-up (they would sit beside the `autoClose` one, which is exactly
the same `{enabled, dryRun}` shape) — but not tonight, and not before Thursday.

**Step 1 — arm the scorer in DRY RUN.**

1. Open the Firestore console at
   <https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/data/~2Fsystem~2Fconfig>
2. Find the **`nflAutoScore`** field. It is a **map**.
   - **If it is not there at all**, click **+ Add field**, name it
     `nflAutoScore`, choose type **map**, and add two boolean children:
     `enabled` and `dryRun`.
3. Set `enabled` to **`true`** (boolean, not the string `"true"`).
4. Set `dryRun` to **`true`**. If that key is absent, **add it as `true`** — a
   missing `dryRun` already means dry (`readJobGate`: `dryRun: cfg?.dryRun !==
   false`), but writing it makes the intent explicit for whoever reads the
   config next.
5. Click **Update**.
6. ⚠️ **Edit the two fields in place. Do NOT paste a replacement map that omits
   `enabled`** — an absent `enabled` reads as `false` and disables the job while
   looking configured.
7. **What you should see:** both values persist after a console refresh, and
   (since #362, `functions/src/systemConfigAudit.ts` →
   `onSystemConfigWritten`) a new entry in the SuperAdmin **Admin Audit Log**
   recording the `system/config` write. That trigger fires on the document, so a
   console edit is captured the same as a UI one.
8. **If the values do not persist:** stop and tell me. A config write that
   silently does not stick is the worst possible state for a kill-switch.

**Step 2 — wait for one 10-minute tick, then read the heartbeat.**

Open
<https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/data/~2Fsystem~2Fheartbeats>
and look at the `nflAutoScoreJob` map:

- `activeSlates` — how many game slates it saw
- `poolsScored` — how many pools it **would have** scored (a dry run writes
  nothing)
- `poolsFailed` — **healthy is 0**
- `poolsSkipped`, `overflow` — informational, not graded

(Field names verified in `functions/src/lib/autoScoreDecisions.ts:235-243`; the
verdict is unhealthy if and only if `poolsFailed > 0`, `:283`.)

**What good looks like before the HOF game:** `activeSlates: 0` and everything
else 0. There is no live slate yet, so a quiet beat proves only that the job
runs. **The dry run is only meaningful once a game is in progress** — that is
Thursday 2026-08-06, 8:00 pm ET.

**Step 3 — flip it live.** Same Firestore console document: set
`nflAutoScore.dryRun` to `false`, leaving `enabled` at `true`. **Do this only
after you have seen a dry-run beat with `poolsFailed: 0` on a real slate.**

**Step 4 — `nflSpreadLock`, dry-run FIRST.** Same document, same method.

Its schedule is Tuesday 9:00 am ET, so nothing you do on Thursday takes effect
until 2026-08-11. The HOF game's spread is already locked at `-1.5`, so this is
for **Preseason Week 1** (2026-08-13) onward, not for Thursday night — which
means you have a free Tuesday to spend on a dry run.

1. Set `nflSpreadLock` to `enabled: true`, `dryRun: true`.
2. After the **Tuesday 9:00 am ET** tick, read the **`lockNFLSpreadsJob`**
   heartbeat (not `nflSpreadLockJob` — see the warning above).
   `lockSpreadsOnce` writes nothing while dry and reports `wouldLock` — the
   number of games it would have locked — plus `overflow`, games that exceeded
   the 200-per-run cap and were skipped (`functions/src/nflSchedule.ts:957-996`).
3. **What good looks like:** `wouldLock` matches the number of upcoming games
   you expect to have a spread, and `overflow: 0`.
4. **If `overflow` is above 0**, the cap is biting and some spreads would NOT
   lock — tell me before arming.
5. Only then set `dryRun: false`.

⚠️ **A live run is not reversible from this job.** It sets `spread.locked: true`
on every eligible upcoming game, and locked spreads are what pick submission is
validated against. There is a manual unlock path in the admin surface, but the
job has no undo.

**Step 5 — `nflFinalize`, dry-run FIRST.** Same document. Set all three keys
together, and note the types: `enabled: true` (boolean), `dryRun` (boolean), and
`liveSeasonTypes` as an **array** containing the single number `1`.

1. Start at `{ enabled: true, dryRun: true, liveSeasonTypes: [1] }`. Include
   `liveSeasonTypes` even in the dry run — the dry-run report breaks its
   candidates down BY seasonType precisely so you can see what a given scope
   would arm before arming it.
2. After the **04:30 ET** run, read the `nflFinalizeSweepJob` heartbeat and the
   dry-run report it writes to `admin_audit`.

   ⚠️ **Read it in the FIRESTORE CONSOLE, not in the app — and expect the
   `admin_audit` row to be a SUMMARY, not the candidate list.** Two reasons.
   The SuperAdmin Admin Audit Log renders five columns — When, Actor, Action,
   Target, Status (`src/components/admin/AdminAuditViewer.tsx:58-62`) — and
   nothing from each record's `metadata`. And `writeAdminAudit` runs every
   record through `capMetadata` (`functions/src/lib/adminAudit.ts:32-54`), which
   replaces arrays and objects with the marker `[array]` / `[object]`, truncates
   strings past 200 characters, and caps the payload at 1 KB. So a per-pool list
   in an audit record is stored as five characters. **For the actual candidate
   detail, read the function logs** for `[nflFinalizeSweep]` around the 04:30 ET
   run.
3. **What good looks like:** the candidate list contains only pools you agree
   are season-complete. A finalized pool is the end of its scoring life, so this
   is the one to actually read rather than skim.
4. Only then set `dryRun: false`, leaving the other two keys as they are.
5. **Verify the arming took:** re-read the heartbeat after the next 04:30 ET
   run. If it still reports dry-run behaviour while the config says
   `dryRun: false`, the `liveSeasonTypes` key did not take — see the refusal
   warning above.

### What is already built for this — you are not asking for new code

`nflAutoScoreJob` already has a fenced scoring lease, per-week fingerprint
idempotency, provisional mid-week passes, and a rescore queue. It was built to be
armed.

⚠️ **Survivor is the one type where ordering matters.** `survivorAllowedForGroup`
(`functions/src/lib/rescoreQueue.ts:255`, called at `nflAutoScore.ts:263`) skips
a Survivor pool while a rescore is queued for it, because eliminations are
order-dependent — scoring week N+1 before a corrected week N eliminates the wrong
people. If a Survivor pool ever looks stuck, that skip is the first thing to
check, and it is deliberate.

## B3. Coolify rebuilds owed

**Dashboard:**
<http://72.60.68.7:8000/project/ycoooow0g4c08ogso404k8o4/environment/ogs0cg0gg0kcgkgc8sg4c8g4/application/ics4kkww0c8oo0gw4wkg8w4o/deployment>
→ **Redeploy**.

### The rebuild HANDOFF says is owed is ALREADY DONE

HANDOFF's STOP POINT box says a rebuild is owed for #358 / #359 / #360 / #348 and
names the live bundle as `index-H9HjG31q.js`. **That is stale.** Measured off
production tonight:

- the live bundle is **`index-bJOPbtJA.js`** (read from
  `document.querySelectorAll('script[src]')` on the prod page)
- the **Production Watchdog** card from #348 renders and returns real data
  ("LAST 24H · AS OF AUG 4, 03:17 AM", 50 client errors)

So you rebuilt after #348 merged. **HANDOFF's STOP POINT box needs its bundle
hash and its "Coolify OWED" line corrected.** I did not edit it: that box is the
deploy record, and rewriting it from a browser observation rather than from a
deploy log is how it went stale in the first place. Fix it when you next touch
it, from the Coolify log.

### ⬛ THE REBUILD THAT IS OWED RIGHT NOW — this is your one action

**#367, #368 and #366 are all merged into `main` and NONE of them is live.** All
three are `src/` or lockfile changes, and the production bundle does not move
until the frontend is rebuilt. One rebuild covers all three.

1. ~~Merge the PRs~~ — ✅ done (`3e11017`, `7da9189`, `eacfc37`).
2. Open the dashboard link above and click **Redeploy**. Wait for it to finish.
3. **Verify it shipped:** open <https://www.marchmeleepools.com>, view source,
   and confirm the `assets/index-*.js` hash is **no longer `index-bJOPbtJA.js`**.
4. ⚠️ A moved hash is positive evidence; an **unmoved** hash is inconclusive, not
   proof of failure — read the Coolify deployment log rather than concluding it
   broke. HANDOFF records why.
5. **Then verify the actual fix**, which is the whole point. Go to
   <https://www.marchmeleepools.com/pricing> and click **BUYFLOW TEST** under
   "Your Trial Pools Awaiting Activation". Look at the right-hand card.
   - **What good looks like:** `Base Hosting fee (25 estimated players)` shows a
     real dollar amount, **not** the word FREE, and the button reads
     `UPGRADE POOL TO PREMIUM` and is clickable.
   - **If it still says FREE:** the rebuild did not ship. Check the Coolify log.
   - **If it says `Pricing Unavailable` in a red notice with a "Try Again"
     button:** the quote is now genuinely failing server-side. That is a
     different bug and I want to know.
6. **Also look at the banner** (#368): open any pool you own whose billing is
   `active` on a paid tier. You should see a **green banner with white text
   reading "HOSTING FEES PAID"** above the pool content.
   ⚠️ **UNVERIFIED** — nobody has looked at this in a browser. Its markup and its
   exact colours are asserted by `renderToStaticMarkup` and the branch logic is
   mutation-tested, but no human and no screenshot has confirmed how it renders.
   One glance, please.

## B4. Dependency PRs — one to merge, two to close

**#366 subsumes NEITHER #354 nor #353.** #366 is the *minor-and-patch* group, and
both of the others are **major** bumps, which that group excludes by definition.

| PR | What | CI | Outcome |
|---|---|---|---|
| **#366** | minor-and-patch group, 11 updates (`firebase` 12.16→12.17, `@sentry/react`, `framer-motion`, `react-hook-form`, `@playwright/test`, `postcss`, type packages) | **all 6 checks pass**, `MERGEABLE / CLEAN` | ✅ **MERGED** `eacfc37` |
| **#354** | `lucide-react` 0.556 → **1.28** (major) | **`build-and-test` FAILS** | ✅ **CLOSED**, reason posted on the PR |
| **#353** | `@vitejs/plugin-react` 5 → **6** (major) | **4 of 6 checks FAIL** | ✅ **CLOSED**, reason posted on the PR |

**Why #354 fails, concretely** — lucide-react 1.x removed the brand icons:

```
src/components/UserProfile.tsx(13,41): error TS2305: Module '"lucide-react"' has no exported member 'Twitter'.
src/components/UserProfile.tsx(13,50): error TS2305: ... has no exported member 'Facebook'.
src/components/UserProfile.tsx(13,60): error TS2305: ... has no exported member 'Linkedin'.
src/components/UserProfile.tsx(13,77): error TS2305: ... has no exported member 'Instagram'.
src/components/modals/ShareModal.tsx(3,18): error TS2305: ... has no exported member 'Twitter'.
```

That is a real migration — every social icon in the app needs replacing. Not a
two-days-before-kickoff job.

✅ **#366 is merged** (`eacfc37`). It changes `package.json` and
`package-lock.json` only, so it needs **a Coolify rebuild** (folded into the one
in §B3) and **no functions deploy** — the functions lockfile is a separate file
this PR does not touch.

✅ **#354 and #353 are closed**, each with the reason posted on the PR so a
future dependabot rebase does not silently reopen the same argument. Neither is
rejected on merit; both are deferred until after the preseason slate settles.

I did not run codex on #366. CLAUDE.md §2c says not to burn paid rounds on
trivial diffs, and a lockfile bump with six green CI checks is that.

## B5. `PLAN-BILLING-ENFORCEMENT.md` — written, NOT implemented, decision yours

New file in this PR. It proposes the kill-switch and dry-run that
`enforceBillingStatus` has never had — the finding three independent reviewers
converged on during #365, deferred there on purpose.

**Why it is a plan and not code:** it is a `functions/` change on a money path.
It owes a functions deploy, and the job is currently working correctly. Two days
before the HOF game, that is not a free action.

The proposal, in short:

- `system/config.billingEnforce`, read through a `readBillingGate` helper with
  the same fail-safe defaults as `readJobGate` — absent config means disabled,
  and anything but an explicit `false` means dry-run
- **plus `nflFinalize`'s refuse-to-arm-unscoped rule**, adapted: a
  `liveTransitions` key naming which of the two phases may write. Phase 1 emails
  a warning; phase 2 **locks the pool for every participant**. Those two should
  not arm on one flag.
- dry-run must **send no email** — a status can be written back, an email cannot
  be unsent

**Three questions for you**, all in §8 of the plan: build it or not; two-key
arming or plain `{enabled, dryRun}`; before or after Thursday. My recommendation
is *build it, two-key, and ship it on the next functions deploy that is already
happening for another reason* — not a dedicated deploy this week.

## B6. Found, and NOT fixed

### 1. ⚠️ The SuperAdmin **Sim** button writes fabricated scores to REAL pools

This was queued as a tab-placement question (task chip `task_1957c853`: the
per-pool Sim button lives in the Pools tab while CONTEXT.md says the Test Suite
is the sole home for simulation tooling). **The placement is the smaller half of
it.**

`handleRunSim` (`src/components/SuperAdmin.tsx:863`) is a hardcoded score-advance
state machine. It calls `dbService.updatePool(pool.id, …)` on the **real pool**:
the first click locks the pool and sets `scores.gameStatus: 'pre'`; subsequent
clicks walk a fixed script — `Home 7, Away 0`, then `Home 7, Away 3`, then end
Q1 — writing invented scores into production. It creates no `sim-` pool and
carries no Run ID, so by CONTEXT.md's own definitions it is not a Sim Run at all.

It renders on **every SQUARES pool row** in the Pools tab
(`src/components/SuperAdmin.tsx:1514`), real or not, behind a confirm that says
only *"This will advance the game state."* Squares is the pool type with
per-quarter money on it.

**My recommendation: delete it.** The Test Suite / TournamentSimulator is the
real harness, CONTEXT.md already says testing lives nowhere else, and a button
that fabricates production scores is worse than a misplaced one.

**I did not implement the removal.** It is a SUPER_ADMIN-only tool you may
actively use for demos; removing it is a judgement call about your own workflow
rather than a defect fix, and it is not on the critical path to Thursday. Say the
word and it is a small PR. The sibling **Fix** button is by design per
`mmp-superadmin-surface` §5 and is not in scope either way.

### 2. The `couponCode` client errors should stop — verify that they do

The 50 watchdog errors in §B1 are the same bug #367 fixes. **After the rebuild,
the Production Watchdog's `CLIENT ERRORS` count over 24 h should drop sharply,
and no new `Invalid request: couponCode` entries should appear.** If they keep
arriving after a confirmed rebuild, the fix did not reach the bundle.

### 3. `TEST-SUITE-BACKLOG.md` — untouched

The parked simulator bugs, including the `bracketSimulator` 0-entries cluster,
were not worked. Lowest priority in the queue, and the night went to the buy
flow.

## B7. Deliberately left alone

- **`poolTypeFlags`** — all seven still `false`. Your "coming soon" posture. Not
  touched, not questioned.
- **`nflAutoScore` / `nflFinalize` / `nflSpreadLock`** — not armed. §B2 is a
  runbook for you, not something I did.
- **`currentSeason: 2025`** — dead field, zero readers, not "fixed".
- **No deploys of any kind** — functions, rules, indexes, Coolify: none. Still
  true after the merges: all three were frontend-only, and **the Coolify rebuild
  is still not done** (§B3).
- **No config or kill-switch flips.** `nflAutoScore`, `nflFinalize`,
  `nflSpreadLock` and the `poolTypeFlags` are exactly as they were.
- **No simulators run against production.**

⚠️ **One line in this list was RETRACTED on 2026-08-05.** It read *"No merges.
#367 and #368 are open and waiting."* That was true when written and stopped
being true when Kevin said to go ahead: #367, #368 and #366 are merged and
#354/#353 are closed (§B1). Retracted in place rather than deleted, so a reader
who saw the original knows it moved.

## B8. Gates, and where I stopped short

**All test counts in this addendum were measured on 2026-08-04.**

**#367** — root vitest 658/658, functions 1334/1334, `tsc -b` clean, lint 0
errors, build succeeds, **all 7 CI checks pass**. codex 5 rounds against a
ceiling of 10, final round clean on the post-qodo diff. qodo REPORTED, both
findings absorbed. Joint stopping rule satisfied on all three legs.

**#368** — root vitest 633/633, `tsc -b` clean, lint 0 errors, build succeeds,
**all 7 CI checks pass**. codex 4 rounds, final clean on the post-qodo diff. qodo
REPORTED: 1 absorbed, 2 rejected with reasoning posted on the PR.

**#369 (this document)** — docs-state invariants 82/82, root vitest 623/623.
**codex 10 rounds, 8 findings, all absorbed**, final round clean on the post-qodo
diff. qodo REPORTED: 2 absorbed, 2 rejected with reasoning posted on the PR.

⚠️ **Ten rounds on a docs-only change is not padding, and it is the finding of
the night worth generalising.** Every one of the eight was an operational
instruction that could not be followed or that promised something the code does
not do: a System tab with no such control (twice), an arming value that would
have *disabled* the job, two jobs armed straight to live in a runbook that
preaches dry-run-first three paragraphs earlier, a heartbeat key that does not
exist, a transition count sold as an email count (twice), and a dry-run report
that `capMetadata` would have stored as the five characters `[array]`.

None of that is visible from reading the prose. **A runbook is code that a human
executes, and it deserves the same review the code gets** — this one would have
stranded Kevin at step 1.

**What I could NOT verify, stated plainly:**

1. **Neither fix has been seen working in a browser.** Both are `src/` and the
   live bundle is pre-merge. Every claim about the *fixed* behaviour comes from
   unit tests, mutation tests and source reading. The **broken** behaviour is
   what was measured on production. §B3 step 5 is the verification, and it is
   yours.
2. **The banner's appearance is unverified.** Colours and markup are asserted by
   `renderToStaticMarkup`; nobody has looked at it.
3. **`system/heartbeats` could not be read directly.** Extracting a Firebase auth
   token from the browser session is blocked by policy and no UI renders that
   document, so every heartbeat claim here is derived from the Ops Health card's
   rendered summary, not from the raw document.
4. **`enforceBillingStatus` has still never completed a run** (§B0). The index
   fix is unproven until tonight.
