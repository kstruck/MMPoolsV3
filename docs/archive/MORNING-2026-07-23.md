# ☀️ MORNING 2026-07-23 — your task list

**Target: the Hall of Fame game, 2026-08-06.** 14 days out.

**No production data was touched. Nothing was deployed.** One PR is open.

---

## 0. The 60-second version

| Order | Task | Section | Time |
|---|---|---|---|
| 1 | Merge the profile-fix PR | §1 | 2 min |
| 2 | **Coolify rebuild** — the fix is frontend, it does not deploy on push | §2 | 5 min |
| 3 | Read the stats plan and answer 6 questions | §3 | 15 min |
| 4 | Check the Firestore reads graph (now overdue — it was due yesterday) | §4 | 2 min |
| — | Sentry item: **no action needed**, explanation in §5 | §5 | — |

**The headline:** your item 2 (SuperAdmin stats) is **much bigger than a
filter**, and I deliberately did not build it. Two rounds of review found five
independent reasons the obvious fix would have written *wrong numbers over a
publicly-readable money document*. §3 has the decisions I need from you.

---

## 1. Merge the profile PR

**Item 1 — profile pages missing headers and footers. Fixed.**

`/profile/:uid` — the public player profile, the URL you shared — rendered
`<PlayerProfile />` with no wrapper. Every sibling route supplies chrome; that
one did neither of the two available ways, and nothing failed. One-line class of
bug, live on the page most likely to be seen by a logged-out visitor.

1. Go to the PR (link in the terminal output / `gh pr list`).
2. Confirm the seven CI checks are green.
3. **Squash and merge** → **Confirm**.

**I verified it visually, not just by test.** I ran the dev server against a
throwaway fake Firebase config and loaded your exact URL: the full nav renders
with LOG IN / GET STARTED for a logged-out viewer, and the four-column footer
renders below the profile. Your real credentials were never copied into the
worktree — the fake `.env` was deleted afterwards.

A guard (`tests/route-chrome-invariants.test.ts`) now fails if that route loses
its chrome again. Verified it *bites*: reverting the fix fails two of its
assertions.

---

## 2. Coolify rebuild ⚠️ REQUIRED

The profile fix is `src/App.tsx`. **Pushing to `main` does not rebuild the
frontend** — it needs a manual trigger, so merging alone leaves the fix invisible.

1. Open the Coolify dashboard.
2. Select the **`www`** application.
3. Confirm **Source** is `main`.
4. Click **Redeploy**.
5. Wait for the container healthcheck to pass.
6. Verify: open https://www.marchmeleepools.com/profile/6C09waBoqiSavoBnPZrMkhxWt7x2
   — you should now see the site header and footer around the profile.
7. If the header is still missing, hard-refresh (Ctrl+F5) first — then tell me.

No functions deploy is needed. Nothing in `functions/` changed.

---

## 3. The stats work — read the plan, answer 6 questions

**`PLAN-STATS-INTEGRITY.md`** (in the repo root, on the PR branch).

This is your item 2 and item 3. It is plan-gated under the rule you approved
yesterday — it touches money, production data, and a **world-readable** document
(`firestore.rules:470` grants `allow read: if true` on `stats/**`, so these
figures are public, not admin-only).

### Why I built nothing

I started to. Review caught five independent problems, each of which alone
defeats the obvious fix:

| # | Finding |
|---|---|
| 1 | **The Overview cards don't read the document I was going to fix.** They render `liveStats`, computed in your browser from every loaded pool. Fixing the Cloud Functions and running Recalculate would not have moved a single number on the page you were looking at. |
| 2 | **`calculatePoolPot` returns zero for NFL season pools.** Pick'em / Survivor / Margin fall through to the squares branch and evaluate to 0. A recalculate would have written zeros for exactly the pools this season is about. |
| 3 | **Legacy test pools carry no marker at all.** The Squares/Props/Playoff runners create pools through the normal path — random ids, no `simRunId`. No filter can find them. Excluding "test pools" would have missed the ones inflating your numbers and *looked* fixed. |
| 4 | **The recompute never visits NFL pools.** It selects `isLocked == true`; NFL pools are created `isLocked: false` (verified, `nflPools.ts:100`) and use per-week locks. |
| 5 | **NFL "paid" state lives in Member Records**, not entry docs — which keep `UNPAID` forever. `entryFee × paid entries` computes zero for legitimately paid pools. |

Every one was found by reviewing a *document*, before code existed. That is the
plan gate you approved earning its keep on its first real use.

### What I need from you

Six questions, all in §5 of the plan. The three that actually gate the work:

- **Q1** — Has any *real* Stripe checkout ever run against a test pool? Decides
  whether Platform Revenue needs fixing at all (it is a separate pipeline from
  prize/charity and may be perfectly fine).
- **Q5** — Which NFL pools count as "real volume"? They never set
  `isLocked: true`, so the rule needs to be something else: `finalizedAt`
  present? `scoredThroughWeek >= 1`? any member marked paid? **This decides what
  prize volume means for the season and I don't want to pick it for you.**
- **Q6** — How far back should test-pool cleanup go? There is a much simpler
  option available: **draw a line and only count pools created from a given date
  forward.** That happens to match "start keeping track for the NFL season"
  exactly, and it avoids a production remediation pass entirely. **This is my
  recommendation** if you're happy to let historical totals go.

Answer those three and I can sequence the work. Q2/Q3/Q4 are smaller.

---

## 4. Firestore reads graph — now overdue

This was due yesterday (2026-07-23 was the "full post-deploy day" date) and got
overtaken by the deploys.

1. Open https://console.firebase.google.com/project/gridiron-gamble-uzuqo/firestore/usage
2. Range: **Last 30 days**.
3. Look at **Jul 22** — the first full day after the bracket-sync fix.
4. **Expected:** a sharp drop toward the pre-Jul-3 baseline (roughly 0–100K/day
   rather than ~1.4M).
5. **If it is still above ~200K/day**, tell me — the 1.4M driver is provably
   dead, so it would be a second cause worth hunting.

---

## 5. Item 4 — the Sentry error. No action needed.

**Verdict: not a bug. It was a deliberate smoke test.**

The error value is `phase2-sentry-smoke-test`. I searched the entire repository
and **every commit on every branch** — that string has never been committed. It
was thrown ad-hoc on 2026-07-17 to verify the Sentry integration worked during
the Phase 2 observability work. It did work. That is the whole event.

The `Header Rendered. User: null Role: undefined` lines around it are not errors
either — they are breadcrumbs from `logger.log` at `Header.tsx:71`, and
`logger.log` is **dev-gated** (`src/utils/logger.ts:14` only calls through when
`import.meta.env.DEV`). Their presence is consistent with that session being a
local dev smoke test rather than production.

**Nothing to fix.** If you want the per-render log gone anyway it is a one-line
change, but it costs nothing in production today.

---

## 6. What I did overnight

| PR | What | Deploy? |
|---|---|---|
| (open) | Deploy-state marker → the evening deploy; public profile header/footer + guard; `PLAN-STATS-INTEGRITY.md` | **Coolify rebuild** |

Also corrected: the deploy-state marker was still pointing at `6ca9e7f`, two
deploys stale — yesterday morning's deploy deferred the marker update to me and
we went into decisions instead. It now reads the evening deploy, and I removed
the bare SHA copies I had pasted around (they sit outside the CI guard and rot
silently).

### Mistakes I made and caught

- **I ran `git checkout origin/main -- .` on a branch with committed work**,
  which reverted my own committed changes in the working tree. Caught
  immediately, reset cleanly, no loss. It was also unnecessary — `src/` was
  identical on both.
- **My first stats plan was aimed at the wrong data path entirely** (§3
  finding 1). My own draft had flagged those metrics as "could not trace" and
  then proposed steps anyway. I should have closed the unknown before writing
  steps, not listed it as a caveat beside them.
- **I kept declaring the deploy queue empty while leaving lower sections saying
  otherwise** — three separate rounds of the same enumeration failure. Fixed by
  grepping for every stale marker rather than fixing the ones I remembered.

---

## 7. Still open, unchanged

- **A8 pricing — DUE 2026-08-06, 14 days.** Still the only calendar-bound item.
- **Backups.** No PITR, no scheduled backups, no Auth export. PITR is one
  checkbox and buys a 7-day recovery floor.
- **qodo is billing-blocked** — reviewed none of the last nine PRs. codex is the
  only working reviewer, which also means the only second opinion.
- **`nflFinalizeSweepJob` has still never completed a production run.** Now runs
  04:30 ET (pinned). Arming it is NFL-6.
